# Ruling 094 — 25 September 2026

> Received 25/09/2026, 16:09:36 (Europe/London) from the CTO on the direct channel. Verbatim, taken from the session transcript, not retyped. Redactions per CONSTRAINTS.md: none needed (no address, no reference, no person's data).

---

This is the CTO: ruling 094. I've now read #310 and it is cleared. Your reading was right in substance, but a merge order is not a review, so this ruling is the read. File this as 094. Nothing here is NOT-FOR-RELAY.

Assumes: main 4b3e9f3; #310 head 388b56b, BEHIND, 7 files, +402/−9.

§1 WHAT I READ: readGuard.ts in full, and persistStore.ts's apply path.
- `read` runs on every payload that will be applied.
- On any fault, the raw bytes are retained under `.retained.shape` BEFORE `store.set`, and the retain is awaited.
- Each fault is recorded, with a console warning. A non-array field becomes []; a non-object entry is dropped and recorded by index.
- That is the 082 §2 property, and it holds.

§2 CLEARED TO MERGE via merge-one.sh after update-branch, on green checks.
Two notes, neither blocking:
- (a) The +27 lines of ENGINEERING.md predate the freeze, so they merge. The Phase 1 handbook rewrite folds them in.
- (b) Retention on failure: if the `.retained.shape` setItem itself throws, check what `retain` does next. If apply proceeds and the next save overwrites the key, the bytes are gone. This is the pre-existing behaviour for every retain reason, so it is not #310's defect. Answer it in one line in your report: which, with the line number. If it is "proceeds", it goes on the Phase 1 list.

§3 THE RULE GOING FORWARD: a PR I haven't named as read and cleared waits for my read. That applies even when it sits in a merge order I wrote, and even when it is green by path. Name the PR in your report and I'll read it the same day.
- The F1 guard PR, #300 and #302 each get my read before merge.
- #300 and #302 were cleared by the previous CTO in 082 §2. I accept that clearance, conditional on the re-carry union check passing, and I read the re-carried heads.
- #309: 086.
- #311: my read, with its guard.
