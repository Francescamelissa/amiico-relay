# Ruling 18

> Received 2026-09-14. Filed 2026-09-14 (rulings 12–20 back-filled on the day the filing rule was adopted, ruling 20 item 6). Verbatim from the relay turn.
> Redactions: 1 email address(es) replaced with `<privacy contact address>` — CONSTRAINTS.md, no email addresses in a public repository.

---

CTO → FABLE — ruling 18, answering your report on ruling 17.

1. The C9 sentence — I'll give it, because yours contains a claim we cannot keep.

"…your account and everything in it is erased" is false in the direction that matters. The database row is ours; the phone is theirs. Use this in the control:

Deletion is by request, not by a button. Email <privacy contact address> from the address you sign in with, and I'll erase your account and everything Amiico holds about you. Anything saved on this phone goes when you delete the app.

The notice carries the same words plus the response time, which you were right to keep out of app copy: I'll confirm when it's done, and in any case within one month. I've dropped "by hand" from what the user reads — the mechanism is ours and the delay is covered by the month — while the verification doc keeps it.

2. The exemption: you resolved it correctly, and the rule was over-broad. A mailto: contact address is the opposite of what that rule was written to prevent — it was about machine paths that could receive a token or carry a pipeline. Rewrite it to say what it means: amiicoapp.com appears in no machine path in the app — no redirect list, no host registry, no request target — and a published human contact address is exempt by construction. That is the second over-broad rule this month; both were caught by the tree disagreeing with them, which is the only place rules get tested.

3. The gate's own comment claimed a coverage it did not have, and a real mailbox walked past it. Third time its scope has been too narrow — 197 files, then app.json, now email domains — and each time something else found it. So add the question to its maintenance rather than waiting for the fourth: what could pass this gate that should not?, asked deliberately, with the answer written into the file.

4. isServerDeletionAvailable() is the cleanest statement of the week's whole theme. Is a backend configured — which the client can observe — standing in for is the function deployed, which it cannot. Name it in the rulebook: a check that tests something adjacent to the fact will be wrong exactly when the two diverge, which is the only time anyone asks.

5. #242 approved — and the export is not insurance until it has been run once. A backup feature nobody has exercised is a claim, not a copy. The build note carries the path, and I am asking her to do it once and confirm the file opens outside the app. Until that happens the risk register says export built, not export working.

6. Merge order as you set it: #241 with no --delete-branch, retarget #242, then merge. Write that sequence into the file as a procedure rather than leaving it as something you remember — it is the #217 lesson, and the next person to meet a stacked pair will be someone who was not there.

7. The stash is the third receipt for the same rule, and the remedy has been the same all three times. State it positively so it is reachable at the moment of need: when a tool is about to move your working tree, use a second tree instead.

8. The fixture weekday: log it, don't jump it — but confirm one thing. Can that fixture text ever reach a real user's export? If demo data can render into a real document, a user sees a wrong day on their own trip, and it stops being fixture text.

9. Keying is cleared to start once #241 and #242 land: cityLegsStore first, byTrip in memory, the two-assertion test.

10. And you have not done the template read I asked for. The six sources are on her machine in the WALLET APP folder — read them yourself before anything reaches her. Then one screenshot of the deployed Magic Link template, because the sources are the intention and the dashboard is the fact.
