# Ruling 095 — 25 September 2026

> Received 25/09/2026, 16:26:39 (Europe/London) from the CTO on the direct channel. Verbatim, taken from the session transcript, not retyped. Redactions per CONSTRAINTS.md: none needed (no address, no reference, no person's data).

---

This is the CTO: ruling 095. I've read #319 and it is cleared. #309 gains condition (f), and `you` joins the next read-guard PR. File this as 095. Nothing here is NOT-FOR-RELAY.

Assumes: main 78bfaf9; #319 head 3359872, 3 files, +219/−2.

§1 WHAT I READ: budgetRead.ts in full, and the registerPersistence diff.
- Every collection the purge and the screens iterate is guarded, and a non-object budget reads as null.
- The registry and the test share BUDGET_PERSIST_OPTIONS, and your M4 proves they can't drift apart.
- No money arithmetic is touched.
- One behaviour to know about, not to change: a MISSING `budget` key is recorded as a fault (found 'undefined') and retains the bytes. Build 18 always wrote `budget: null`, so in practice it's silent. If a missing key is ever normal, that's a fault every boot.

§2 #319: CLEARED TO MERGE via merge-one.sh on green checks. Body: "## CTO clearance / ruling: 095".

§3 #309 GAINS CONDITION (f), added to 086 §3.
- readBudgetState reads the v1 envelope: members, thresholdsFired, a single budget.
- #309 migrates to v2: budgets[], alerts[], no members, no thresholdsFired.
- Since `read` runs on the migrated payload, the v1 read on v2 data would:
  - record a fault on every boot, for thresholdsFired and members missing;
  - retain the bytes every boot;
  - write `members: []`, `budget: null` and `thresholdsFired: []` into the v2 state;
  - leave `budgets` and `alerts` unguarded.
- So #309's re-carry REPLACES readBudgetState with a v2 read: guarded lists for budgets, expenses, settlements and alerts, with each alert's thresholdsFired as a guarded number list.
- The same M-style reds apply, including "good v2 data records no fault". Paste the test names and the red matrix with #309's paste.

§4 WHERE THE REMAINING READS GO. One PR after #300 and #302 guards the boot reads of wallet, itinerary AND `you`: row 89's second half, with `you` added. Same pattern, same shared-options wiring, same reds.

§5 THE RETAIN-FAILURE FINDING (094 §2(b)): on the Phase 1 list. If `.retained.*` can't be written, apply must NOT proceed. The store stays unhydrated in memory, the key stays untouched, and the failure is recorded and surfaced, not swallowed. It's a small, high-value data-safety fix; it goes in Phase 1's early PRs.

§6 NEXT, as you said: re-carry #300, run the union check on every hand-resolved file, and report the head for my read.
