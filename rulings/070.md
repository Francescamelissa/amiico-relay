# Ruling 070 — the overnight brief: no cut, the sighting keeps its shape, the merge queue with the union check, the keep-alive, the audit re-read, unattended failure, the clearance extension, conversion v1

> Received 2026-09-21 20:37:45 BST, untitled and unnumbered; 4 sections. **Ordinal 070 assigned by the builder from the directory.** Verbatim.

---

Overnight, unattended. Four sections, §A to §D.

She is asleep and nothing reaches me until morning. Every decision that needs a CTO waits.

§A — standing rules, and one that matters more tonight than any other

Build 19 cannot be cut tonight. The migrations-live gate is red MISSING, it is gate 0 of a cut, and only her paste clears it. That is correct and it is not an obstacle to be worked around.

In particular: do not convert my sighting into a ## Reading. You filed it deliberately outside the shape the gate parses, and that judgement was exactly right. A gate is never made green by re-labelling the evidence — that is the one move that would destroy the value of everything built this week, and it would be available to you at about two in the morning with build 19 blocked behind it. The red stays until a human reads the live database.

Otherwise as always: committed increments, every worktree through the helper, every validator || exit 1, all writes through the write helper, nothing touches production, no migration merges, no cut, no submission. When a decision needs me, park it with the question stated and move on — a night built on a guess I would have refused is worse than a night with an item parked. If the composite goes red and you cannot make it green, stop and write up rather than stacking on it.

§B — the order of work

1. Finish the merge queue. #275's re-carry with the union check from 068 — every entry present on either parent present in the result, asserted, not eyeballed. Then #284, #277, #278, #279, #281, #282. #280 stays open; it is mine to read and a merge tonight would erase the reason it was held.

2. The keepalive, both artefacts. Workflow deleted, secret deleted, the secrets list read back and recorded as the evidence of absence. Inventory row 19 rewritten with what is actually true — never succeeded, eighteen for eighteen, purpose never established — and the pause risk stated plainly as a thing nothing currently prevents.

3. The audit re-read. Every row marked established: does its artefact show the mechanism succeeded, or only that it exists? Rows citing a file, a config entry or a setting's presence are demoted to assumed and say why. Expect more than one. Row 19 will not have been the only one.

4. Unattended failure — the separate finding, and then a fix. First establish where a scheduled-workflow failure notification currently goes, if anywhere, from the actual settings rather than from how GitHub is supposed to behave. Then build a path that works: a step that opens an issue on failure is free, needs no external service, and reaches a person's inbox. Demonstrate it by failing a job on purpose and confirming the issue appears — a notification path that has never delivered is exactly the thing we just spent a day on.

5. The ## CTO clearance extension. Per my ruling on row 61: red-lane pull requests carry a clearance section naming the ruling that cleared them, read fresh at merge alongside the deployment conditions. Fixture cases through the fake gh, including the refusal. It enforces recollection, not authority — write that in the file so nobody later mistakes it for a permission system.

6. Conversion, version one — her feature. She asked for this on 15 September and it has waited behind an incident ever since. The design is written and the home_currency rename that blocked it is done. Build it: a user-entered rate per trip, no external service, display-only. The rate is stored as a rate with the date it was entered; a converted figure is never stored as money, never summed, never read by anything that treats it as authoritative. Every converted figure prints its rate and that date beside it. Money stays integer minor units in its own currency, displayed through the existing formatter, asserted on exact strings. PR left open for my read — it is user-visible and it touches money.

7. If all of that lands, take the queue top-down. Anything user-visible or red-lane opens a PR and waits.

§C — not yours tonight

The cut, the submission, any production access, any migration merge, the erasure in any form, a rate provider or any other external service, #280's merge, and the sighting's shape.

§D — the morning report

Manifest first. Then item by item with its done-condition and the evidence that met it. Then what was parked and the exact question each waits on. Then the composite's final state — gates, reds, timing, toolchain. Then the audit re-read in full, with every demotion and its reason, because after tonight that table is the most load-bearing document we have and I want to see what a second pass did to it.
