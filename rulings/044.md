# Ruling 044 — the partition's first row and the window's boundary; the self-retiring arm is testable both ways; ~/.zshenv is configuration outside the repository; did the composite run on main; enforce_admins as a verified fact

> Received 2026-09-15 21:39:42 BST, untitled and unnumbered; five sections §1–§5. **Ordinal 044 assigned by the builder from the directory.** Verbatim.

---

§1 — one row of the partition sits on the wrong side of your own boundary

You defined the window as every invocation from 20:24:45. The first row of the table is timed 20:24:21 — twenty-four seconds earlier — and is attributed to a dead python3. Both cannot be right.

Three possibilities and they have different consequences. The timestamp may be the composite run rather than the edit, in which case the row conflates two events and needs splitting. Or /Applications/Xcode.app's mtime is when the install finished while the licence gate began biting earlier, in which case the window starts before 20:24:45 and your enumeration of forty-two calls is drawn against the wrong boundary — everything between the real start and 20:24:45 was excluded from the taint review by definition. Or the attribution on that row is simply wrong.

The partition is a good piece of work and it is exactly what I asked for; this is the one place where the boundary is doing the work and the evidence disagrees with it. Resolve it before it hardens into the record.

§2 — the self-retiring arm is testable, and it is the part that most needs testing

"Its only true test is the licence acceptance itself" is an overclaim. The predicate is does /usr/bin/git succeed — you can drive both branches today by pointing the same logic at a git that works and at one that does not, without waiting on her and without touching the real condition.

Test both directions, because both fail badly and neither fails loudly. Failing to retire leaves a stale override in place indefinitely, and nobody notices because everything works. Retiring early breaks every call at once. A mechanism that fires once, in the future, unattended, is the one that most needs a demonstrated red — precisely because nobody will be watching the moment it goes.

§3 — ~/.zshenv is now load-bearing configuration living outside the repository

Guarding it to CLAUDECODE shells and verifying her Terminal reads unset was careful and right. But the build now depends on a file that is not in version control, not reviewed, and not recorded anywhere — which is the same class of problem as a setting in a web console, in a new location.

Give it a row in docs/console-control-plane.md with its current contents and what depends on it. The composite printing the toolchain block records what was used; the inventory records what the environment says. Those are two different facts and this evening is a demonstration of why the difference matters.

§4 — did the composite run on main?

Your read-back confirms every new file is present and the four parents count. That is file presence on the squashed tree, which is not the same as eighteen gates green on the squashed tree — and the squashed tree is one that has never been tested anywhere. If the composite has not run on main since bfa5bbf, run it: alone, under the toolchain, per-gate timing recorded.

§5 — what I endorse

enforce_admins = true on main is the single most important line in your report. That is the property I was actually worried about — a control that exempts the owner binds nobody who uses this repository — and it turns out to have been right all along. Record it as a verified fact rather than a happy discovery, because it is now load-bearing for the whole force-push argument.

And the floor at 190 with its reason written beside the number and a direction of travel — raise as the corpus grows, never lower — is better than the number alone. A threshold with no rationale gets lowered by whoever meets it at an inconvenient hour, and they will be able to tell themselves it was arbitrary, because it was.
