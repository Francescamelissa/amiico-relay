# Ruling 101 — 25 September 2026

> Received 25/09/2026, 17:33:55 (Europe/London) from the CTO on the direct channel. Verbatim, taken from the session transcript, not retyped. Redactions per CONSTRAINTS.md: none needed (no address, no reference, no person's data).

---

This is the CTO: ruling 101. #320 at head 3686294 is CLEARED TO MERGE. File this as 101. Nothing here is NOT-FOR-RELAY.

Assumes: main 6f3a7e0; #320 head 3686294, CLEAN, 5 files; CI run 36160612548 4/4.

§1 WHAT I READ: youRead.ts in full, and the file list.
- YOU_READ_FALLBACKS is per field and commented as the corrupt-record repair.
- Consent is derived from CONSENT_KINDS, so a new kind can't slip past.
- Notifications fall back all-false, and the profile falls back to REAL, never the demo.
- initialYouState is untouched.
- Your M-PRIVATE mutant reddens only the private-default test. That's exactly the attribution 100 §4 asked for.

§2 A KNOWN LIMIT. It doesn't block. Write it into the PR body as a limit, not a defect.
- guardedObject checks that each nested record IS an object, not that it's complete.
- `consent: {}`, or `notifications: { primed: true }` with the other keys missing, passes through, and a screen reading `consent.analytics.granted` would throw.
- The app always writes these records whole, so the class needs corruption inside a valid object.
- It goes on the Phase 1 list beside the retain-failure item, as "per-key fallback for nested records". Not in this PR.

§3 MERGE via merge-one.sh. Body: "## CTO clearance / ruling: 101".

§4 NEXT, as you listed:
1. #311's re-carry, with its guard shown red on the old branch in a scratch tree, then green. Report the head for my read.
2. Then build 20, per 098 §4.
