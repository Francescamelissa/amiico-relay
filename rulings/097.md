# Ruling 097 — 25 September 2026

> Received 25/09/2026, 16:53:46 (Europe/London) from the CTO on the direct channel. Verbatim, taken from the session transcript, not retyped. Redactions per CONSTRAINTS.md: none needed (no address, no reference, no person's data).

---

This is the CTO: ruling 097. #302's re-carried head a6ee3d9 is CLEARED TO MERGE. File this as 097. Nothing here is NOT-FOR-RELAY.

Assumes: main 1274a22; #302 head a6ee3d9, CLEAN, merge base e26bbda.

§1 WHAT I CHECKED MYSELF:
- (a) The compare-set, derived independently. 9f0beb9→a6ee3d9 is 66 files, and main since e26bbda is 66 files. The difference is empty in both directions, so nothing of #302's own changed.
- (b) The delete-trip purge at a6ee3d9 (tripLifecycle.ts:252–269), read by hand:
  - legs, conversion, activity, itinerary and wallet are purged whichever trip is current;
  - the wallet's projections and the still-unkeyed budget are reset only when the deleted trip was current. That is correct until #309 keys the budget.
- (c) Your markdown handling was right: the tool refused an empty union rather than pass it. The three dropped headings are each superseded by the other side's DONE entry.

§2 MERGE via merge-one.sh. Body: "## CTO clearance / ruling: 082 (code), 097 (re-carried head a6ee3d9)".

§3 NEXT:
1. #309's re-carry with 086 §3 (a)–(f), including the v2 read.
2. Send the paste: the (1b) pin with its red, the corrected body, and the v2-read test names with their red matrix, including "good v2 data records no fault".
3. #309 needs my read of the re-carried head AND the paste before merge. It is money.
4. Then the read-guard PR for wallet, itinerary and `you` (095 §4).
