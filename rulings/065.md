# Ruling 065 — the hold lifts; remove the CLI link; build 19 authorised; the third client of a rename is the bytes already on the device; the day list stands

> Received 2026-09-21 07:38:43 BST, untitled and unnumbered; 3 sections. **Ordinal 065 assigned by the builder from the directory.** Verbatim.

---

§1 — the hold lifts; #272 and #273 merge; and remove the link

Answered properly, from artefacts. No command of yours, the mechanism established from the repository's own record, timestamps from the API. The hold is lifted.

One thing your own forensics surfaced that is worth acting on rather than filing. supabase/.temp/project-ref still points that Mac's CLI at production. It was not involved and you have not used it since 3 July — but the boundary held this week by your discipline, not by construction, and the deploy path is the GitHub App, so the link now serves no purpose at all. Remove it, and record the removal. A capability with no remaining use and a catastrophic failure mode is not a risk to manage, it is a thing to delete.

§2 — build 19 authorised, and the third client nobody has counted

Cut it. But there is a client of that rename that neither of us has mentioned, and it is the one holding her data.

The persisted store on her phone was written by build 18, in the old shape, with home_currency in it. Build 19's code reads currency everywhere — forty-nine sites across thirty-two files. So when build 19 opens, it meets a local store whose fields it does not recognise, and depending on what the version logic does, her Mexico trip either renders with an empty currency, or is retained-but-unused under a version key, or worse. Her trip's contents exist in exactly one place and this is the moment they are most at risk — including the budget and the itinerary we used to prove the export.

So: a rename has three clients, not two. The server, the shipped app, and the bytes already written to disk on the device. Build 19 needs a read-time migration from the old field name to the new one, and it needs to be tested against a store snapshot written by build 18, not by a fixture someone wrote today. That test is the condition; without it, build 19 is more dangerous to her than build 18 is.

Add it to the rule you wrote up: the two-deployments shape has to name the on-device store as the third reader, because it is the one that cannot be redeployed and cannot be queried.

Everything else for build 19 stands as in item 6 of the day list: notes into a repository file first, IPA verified before submission, and the submission held for my read of the column comparison.

§3 — the day list stands

Items 1 through 11 as sent, with item 3's comparison now covering the persisted store alongside the live schema, and item 5's fallback message informed by what you found — the hydration failure is sticky: it falls back once and never asks again. That makes it worse than a transient network error and the message should stop implying it is one.

Your §2 is the best piece of work in this engagement so far: the IPA opened, the exact string found, every path enumerated, and the two failures separated from the three non-failures. That is the standard.
