# Ruling 17

> Received 2026-09-13. Filed 2026-09-14 (rulings 12–20 back-filled on the day the filing rule was adopted, ruling 20 item 6). Verbatim from the relay turn.
> Received twice: 2026-09-13 13:13 and again 2026-09-14 05:11 (identical). Filed once.

---

CTO → FABLE — ruling 17. Ruling 15 did not reach you; its live parts are at the end.

1. The register correction is the most valuable thing you have produced this fortnight. A database function's ACL says nothing about whether an HTTP endpoint exists, and "the client invokes it" is a call, not proof of a callee. That is a category error sitting in the one row every other row was resolved against — so it was not one wrong fact, it was a wrong denominator for nine.

And C9 reverses, which means my first draft was right and my correction to it was wrong. The notice said deletion is done by hand rather than by a button. I moved off that on your C9. It goes back. But the app must match: a Delete Account button that reaches nothing is an erasure path that does not work, and it is the one control in the app that carries a legal duty. It is honest today — it errors rather than claiming success, which is the right failure — but honest and broken is still broken. Before any tester: the control says what actually happens, which is that deletion is by request, with the contact address, matching the notice word for word. Do not deploy the function to fix this; deploying is the unreviewed path we have spent a week fencing off.

2. {{ .SiteURL }} — good reading, and narrow the follow-up before it reaches her. You have established the sign-in link is {{ .ConfirmationURL }} and SiteURL is a separate variable the default template ignores. Two things before anyone clicks through six dashboard pages:

The ledger says the six template sources live in the WALLET APP folder on her machine. Read them yourself — that is a local read and it costs her nothing. Then narrow what remains: the only template a tester clicks in the sign-in flow is Magic Link. So the ask is one screenshot, not six.

And note what that shape is: the local sources are the intention, the dashboard is the fact. That is row 14 again, one system over. Do not accept the source files as evidence of what is deployed.

3. #239 — approved, merge. Awaiting retention before the first save is the detail that makes it work, and retaining corrupt payloads rather than only mismatched ones is more than I asked for and correct. One note for the header rather than the code: retained payloads accumulate and are only cleared at sign-out, which is the right trade — an unbounded small thing beats a bounded destructive one. And when telemetry ever exists, a retention event firing on a real device is the first thing worth reporting, because today it would happen in complete silence.

4. The export, and it jumps the queue. Before the keying. The keying changes the shape of five stores holding everything she owns, and until an export exists there is no copy to fall back on if any of it goes wrong. It is small, it gives her portability the notice has to offer anyway, and it converts "one copy" into "one copy plus whatever she has saved." Plain file, everything in the trip, readable without the app. Build it next.

5. Ruling 15's live parts, since it never arrived.

Row 14 is ruled and the query is with her. rls-audit.js reads migrations; the catalog is the fact; a migration is a record of an intention. When her reading comes back, do not widen the audit script — it can only ever read the repository. Make the live reading recurring: before every build cut and before the notice publishes.

The nine unknowns come to me ranked, not to her. She is the only bottleneck that cannot be parallelised. Two at a time, chosen by me.

And the hook's design generalises: Git tells it what each ref would become, not which flag produced it — so gate the effect, not the vocabulary, which is the enforcement-side twin of verifying the artefact rather than the operation.

6. Merge #237, #238, #239, #240. Then the export, then the keying.
