# Daytime brief — 14 September 2026

> Received 2026-09-14, mid-turn, from the CTO with the founder at work. Not numbered — a brief carrying rulings rather than a ruling — filed dated so the sequence shows it. Verbatim except one redaction per CONSTRAINTS.md (none needed: the brief names no address).

---

AMIICO — DAYTIME, 14 SEPTEMBER. Francesca is at work and cannot relay. Nothing below needs her.
Standing constraints. Red lane held for me: money, RLS, auth, migrations, new sync entities, personal data, any new external service, any new or upgraded native module. No dashboard changes. No Edge Function deploys. No DNS. No secret value in any chat. Nothing published. The force-push hook is live — if it stops you, it is right and the remedy is in it. Reads were never blocked.
1. File the readings first — they are the thing that unblocks a build. All three arrived today, 14 September, from the live catalog on `main / production`:

* 23 tables in `public`, every one `relrowsecurity = true`, every one carrying at least one policy. The count matches the 23 your two parsers derived independently, so nothing is missing and nothing is live that the migrations do not know about.
* Exactly two policies have a `true` qualifier: `job_runs` and `storage_purge_queue`, both `SELECT`, both `PERMISSIVE`, both `{service_role}`, both with no write rule.
* Put them in `docs/rls-live-readings.md` under build 18 with today's date and the query used.

2. Amend the gate before building it — ruling 22. As I first specified it, the condition would redden on those two policies on every run forever, which is a gate that cries wolf. The condition is: permissive, `true` in `qual` or `with_check`, and applying to a role other than `service_role`. The two above get registered by name with their justification, exactly like the host registry, and a third one reddens.
Then two questions I want answered from the migrations, not from the catalog: what do those two policies say they should be, and is SELECT-only intended? Nothing can currently write to either table. A queue nothing can enqueue into is either correct by design or a defect that surfaces the day the functions deploy, and I would rather know which before that day.
3. Merges, in this order and no other. #241 without `--delete-branch` while #242 is stacked on it; retarget #242 to `main`; merge #242; then #243.
4. Cut build 18. It carries the corrected Delete Account control, the export, and the retention fix. Release notes through the gate, written for her — the two lines that matter are that Delete Account now explains deletion is by request, and that Export → Save all my data writes a real file she can keep. One submission, no re-cut.
5. Then the cover photo, and start with the falsifier. The disappearance is unexplained: not the discard-on-mismatch, not a reinstall. My hypothesis is that the cover store persists an absolute file URI, and on iOS the app container path contains a UUID that changes when the app is replaced by a new build — so the path goes stale at a build boundary while the file may still exist. Check what the store actually persists before spending anything on it. If it holds a relative path, or base64, or no path, I am wrong and I want to know rather than have you build on my sixth guess this fortnight. If I am right, the same defect applies to every file reference the app stores, including the export's cached copy, and the fix is to persist relative and resolve at read time.
6. Then the keying, for whatever is left of the day. `cityLegsStore` first — it is authoritative and unkeyed, so this is a first keying, not a conversion. `byTrip` derived at hydrate and held in memory, never written to disk. The two-assertion test: grouped access is correct, and after a write the persisted envelope still holds exactly `legs` with untouched entries byte-identical. And the migration shape stands: write the new shape alongside, never remove the old key, loud failure in CI rather than on her phone.
7. Two small things. Confirm the fixture weekday text cannot reach a real user's export — if demo data can render into a real document, it stops being fixture text. And file every ruling in `rulings/` in the relay repo as you receive it, numbered, so a crossing shows as a gap rather than as two of us repeating ourselves.
Blocked on her, do not chase: the Resend page readings (#20 tracking, #21 webhooks), the backup retention reading (#16), the Magic Link screenshot, and running the export once on a real device. I will hand her two of those when she is back, not five.
