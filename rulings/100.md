# Ruling 100 — 25 September 2026

> Received 25/09/2026, 17:18:23 (Europe/London) from the CTO on the direct channel. Verbatim, taken from the session transcript, not retyped. Redactions per CONSTRAINTS.md: none needed (no address, no reference, no person's data).

---

This is the CTO: ruling 100. A corrupt `notifications` record reads as all-false. Today's defaults stay unchanged. File this as 100. Nothing here is NOT-FOR-RELAY.

§1 MY ERROR, recorded where I made it. 099 §2 named EMPTY_YOU_STATE as though it existed. It doesn't: 0 hits, with EMPTY_WALLET_STATE's 47 as the control. I wrote from the pattern in the other stores without reading `you`'s. Stopping was the right call, and so was building the rest.

§2 THE NOTIFICATIONS FALLBACK: ALL-FALSE.
- A corrupt record means we don't know what she chose. Guessing "on" would invent a choice, in the direction that would start doing something unasked.
- So a non-object `notifications` reads as primed, dailyDigest, morningBrief and urgentChanges all false. Record the fault and retain the bytes.

§3 TODAY'S DEFAULTS STAY: morningBrief and urgentChanges on, on first run.
- That's a product choice about the wedge, and nothing sends today.
- The operating system's own permission prompt will be the real gate when push exists.
- It goes on the plan page as a question to settle when notifications are built. No PR now.

§4 NAMING, to stop the constant-beside-computation trap:
- Don't create an EMPTY_YOU_STATE that disagrees with initialYouState on notifications. Two "empty" states that differ is the defect ENGINEERING warns about.
- Name the repair values for what they are, e.g. YOU_READ_FALLBACKS, per field, with one line saying they are what a corrupt record reads as, not the first-run state:
  - profile: REAL_YOU_PROFILE, never the demo;
  - emergencyContacts: [];
  - privacy: budgetVisibleToCompanions false;
  - consent: all three `{ granted: false, decidedAt: null }`;
  - notifications: all four false.
- The private-default test asserts each of these, and the M-private mutant flips one.

§5 Everything in your "meanwhile" list is as you scoped it. Open the PR when ready, and I'll read it.
