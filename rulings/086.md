# Ruling 086 — 24 September 2026

> Received 2026-09-24 19:35:32 BST from the CTO, on the direct channel — not relayed by hand. Francesca confirmed the channel in the builder's chat before filing (2026-09-24). Verbatim, taken from the session transcript, not retyped. Redactions per CONSTRAINTS.md: 1 link to a private claude.ai page (the plan of record) replaced with `<plan page link>`; nothing else altered.

---

This is the CTO: ruling 086 clears #312 and #309 on conditions and gives the Phase 0 orders. It takes effect when Francesca confirms this channel in your chat. Your filing discipline is correct; hold until she confirms. Nothing here is NOT-FOR-RELAY.

This ruling assumes: main = 8d39087; #312 head = 1847b19; #309 head = f9bcb6f; build 19 not cut; your tree is on feat/rls-reading-grouped and clean. If any of that has moved, tell me which, and what is true now, before you act on the section it affects.

§1 MY CORRECTION, recorded here in the same place I made the claim.
- In 084 I wrote "81 create/alter policy statements". That was wrong. It is 80 `create policy`, 0 alter, 8 drop, so 72 live. The 81st line was a comment.
- The mechanism: I counted grep lines, not statements.
- Also: my 084 conclusion that the rename could not have left a policy on the old name is now established from the evidence, not from my reasoning. The drop at 20260916200000:87 has no cascade, and it applied.

§2 BUILD 19: THE ROUTE. #312 is CLEARED TO MERGE as it is, smoke step included.
- Why: it replaces four reads with one. It files her CSV verbatim, removing the hand-rendering step route (a) needed. It is already proven on real Postgres.
- The smoke passes the freeze test: it has a real failure behind it (the 23 Sep query/doc divergence), and it consolidates rather than adds.
- I verified this myself: the doc fence at 1847b19 is 1,203 bytes, sha256 prefix fac53533efc280b9, and it reads catalogue objects only.
- After the merge:
  1. You post the query to Francesca in your own chat, printed from the merged doc's fence (state its sha256), together with the one console line (Plan · Backups · PITR).
  2. Give her the exact answer shape: SQL editor → run → Export → CSV → paste the file's whole contents back, no screenshot.
  3. She pastes into your chat. You file it verbatim and run the gate. If the editor's CSV quoting of `"` inside the JSON reds the gate, report the red and its first failing row. Do not adjust the input.
- Then the cut procedure unchanged: composite alone and timed; `eas build` from main, commit read back; IPA read expecting 0·0·0·1; one submission; notes pasted and recorded.

§3 #309: CLEARED TO MERGE on five conditions. Clearance to MERGE, not to run anything against production. There is no migration in it.
- (a) Pin the unpinned property (1b): one test feeding `expenses: null` and `expenses: {}`, preserved rather than coerced. Show its red under the coercing break, then green.
- (b) Correct the PR body: 4 of 10; 15 files rewritten / 18 with fewer direct calls; per-property reds as run on 24 Sep; your resolver result (74 statements, 186 symbols, 0 wrong-module, with the planted-control red).
- (c) The two coercions by design are ACCEPTED: non-object `budget` → [], non-array `thresholdsFired` → []. The v1 bytes are retained under .retained.v1, so nothing is destroyed, and a re-fired nudge is harmless. State this acceptance in the body with this ruling's number.
- (d) F1 is fixed and merged BEFORE #309. The boot purge, and every boot-time reader of the budget envelope, treat a missing or non-array collection as empty, retained and recorded: the #310 pattern, as a small separate PR. Show the TypeError red on the old code (in a scratch tree), then green.
- (e) Merge order after the cut: #310 → the F1 guard → #300 → #302 → #309 → #311, plus the guard #311 lacks. Union check on every hand re-carry. The body carries "## CTO clearance / ruling: 086".
- What would make me withdraw this clearance: any edit to money.test.ts or state.test.ts; any change to the 9 money functions; or the money control no longer reddening under the `() => true` filter break.

§4 #313 (brief archive). Ruling:
- It is a signpost to remove now, and a feature for later.
- Remove the Past briefs entry point in the Phase 0 honesty sweep.
- Keep #313 open, relabelled as a post-launch feature (persisted archive + a writer), not a defect.

§5 084 §3(b): don't build the rename seam. Accepted as established by the non-cascade drop plus Postgres's design, as you stated. Revisit only if a future migration renames a column a policy reads.

§6 PHASE 0 ORDERS, 25 Sep – 2 Oct. After build 19, in this order, one PR each. Each PR states the user action performed on the simulator and what was seen.
- B1 · Sign-out that cannot destroy by accident (supersedes #178; close #178 with a pointer).
  - A confirmation step that says plainly what is stored only on this phone.
  - ONE registry-driven reset that clears every persisted store AND stops its listeners, derived from the registry, not a hand list. Today you, budget, wallet, itinerary, brief, activity and tripConversion are not reset.
  - Proof: cancel keeps everything. Confirm empties every store. Sign in as a second test account and see nothing of the first.
- B2 · Money IDs and money input. RED LANE (money). Paste the test names and assertions to me before merge.
  - Expense and settlement IDs from expo-crypto randomUUID.
  - A read-time repair, retained and recorded, for duplicate IDs already on a device.
  - ONE string-based decimal parser replacing the float parse in add-expense, settle and set-budget. The test list covers "1,234.50", "12,50", "0.005" (3 decimals for a 2-decimal currency), empty, and junk. For each, state what you chose (refuse, or a value) and why. Silent zero is not acceptable.
- B3 · Saved places. Include them in the sign-out reset, delete-trip and the export. Derive the persisted-key list from the registry, so the empty lockstep recorded in ENGINEERING.md cannot recur.
- B4 · Honesty sweep. Remove:
  - Home's "We'll read the dates and build the trip for you";
  - "Files your confirmations as they arrive";
  - the Past briefs entry;
  - `multiDeviceSync` from the premium list;
  - the false comment at app/_layout.tsx:74-76.
  List every user-visible string you removed or changed, in all 6 locales. Leave the email's code line: code entry (verifyOtp) makes it true in Phase 1, and changing the hosted template twice is waste.
- B5 · PR triage. Close #50, #100, #104, #109, #110, #140, #141, #171, #172, #178 and #190, each with a one-line reason. Anything worth keeping becomes a GitHub issue with the PR link. Check #140 against #181 before closing. Do NOT delete any branch yet; send me the list of the 108 live branches without a PR first.

§7 THE BIBLE. Read it now: <plan page link>
- It is also a claim about the tree, so report every place it disagrees with what you know. One line each: what it says, what is true, the evidence.
- It is my document. I correct it.

§8 CORRECTIONS. Noted, all five. The pre-commit hook one also goes into the handbook rewrite in Phase 1.

PARKED (for Francesca, via me): confirm this channel in your chat; later, run the reading you send her; paste the build notes into App Store Connect.
