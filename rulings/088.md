# Ruling 088 — 24 September 2026

> Received 2026-09-24 22:36:11 BST from the CTO on the direct channel. Verbatim, taken from the session transcript, not retyped. Redactions per CONSTRAINTS.md: none needed (no address, no reference, no person's data).

---

This is the CTO: ruling 088. #315 is cleared, and build 19 goes through the normal submit with one submission. §3 needs an answer from you before anything else touches a console. File this as 088. Nothing here is NOT-FOR-RELAY.

Assumes: main = 71339ca; #315 head = 5135a8f, docs only (+5/−3); EAS build 46b15327, commit 71339ca, not submitted. If any of that has moved, say so first.

§1 MY ERROR, recorded where I made it.
086 §2 said the IPA "expecting 0·0·0·1". I took that premise from the handoff without checking it against the third-client migration, which I knew existed: ENGINEERING's own rule is that a rename has three clients. I verified your account at 71339ca:
- currentTripMigrations.ts:25–29 names `home_currency`, and registerPersistence.ts:112/388 wires it in;
- the store-key prefix is a runtime constant (persistStore.ts:64), so the full key can never appear in the bundle.
The correct expectation is 1·0·0·1, with the 1 coming from the migration. Your build-18 control was the right move.

§2 #315: CLEARED TO MERGE.
- Then run the composite alone, and `scripts/submit-build.sh 19 46b15327…`. One submission.
- If any gate inside submit-build reds, stop and report. Do not re-cut.
- Add one line to the expect file's commit message: `home_currency` present is meaningful only together with the old select absent. That pair, not the single string, is the discrimination.

§3 DRIVING FRANCESCA'S LOGGED-IN CONSOLES. Answer this before you use any console again, including App Store Connect for the notes.
You drove her production Supabase dashboard in her Chrome. She asked you to, and that is her right as controller. But it moves a person-held step into an assistant's context, and the standing rule is: no person's data but hers, and no secret value, in any chat. So, from the artefacts, tell me:
- (a) every dashboard URL or page you opened, in order;
- (b) the tools you used on each (screenshot, page-text read, DOM/API);
- (c) whether any of them rendered table rows, the Authentication users list, logs, the API keys page, Storage contents, function secrets, or any value belonging to a person other than Francesca;
- (d) whether any of that is now in your transcript.
If (c) is yes: say which. I'll draft a breach-log entry and she confirms it. Nothing further is needed from you.

Standing rule, until the read-only credential in §4 exists:
- Driving her consoles needs her explicit ask per task. It is limited to pages that cannot show another person's data or a secret:
  - Supabase: the SQL editor, running a pre-verified catalogue-only query whose sha256 you check before running; and the Backups settings page.
  - App Store Connect: the build's "What to Test" field only.
- NEVER: table editor, Authentication/Users, Logs, API settings or keys, Storage browser, function secrets, the TestFlight testers list, billing, DNS.
- Read page text in preference to screenshots, and take no screenshot of anything outside those pages.
- NEVER change a setting in any console, including repo visibility, the Supabase plan and Resend. Account settings are hers to change with her own hands.

§4 PHASE 1 DESIGN ITEM, brought forward. The item: a catalogue-only database role for CI to take the RLS reading itself. Design it; don't build it.
- Requirements:
  - it can read pg_catalog and pg_policies;
  - it has NO select on any public, auth or storage table, so it cannot return personal data by construction;
  - it has a demonstrated refusal on a real table.
- The hard part: its password must never enter the repo or a chat. Propose how it is created and put into a GitHub Actions secret by Francesca's hands only.
- This retires both her step and the browser route.
- Send me the design with the rest of Phase 1. It is red lane.

§5 THE READING ITSELF. Accepted as filed: 31 tables, 72 policies equal to the migrations', 2 registered true policies, pg_cron absent, Plan Free / no backups / no PITR. The Plan line matches the page's D2, which is still hers to decide.

PARKED (Francesca, via me): paste the build-19 notes into App Store Connect, unless she asks you to and §3 is answered clean; then install and do the five checks.
