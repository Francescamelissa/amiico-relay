# Ruling 093 — 25 September 2026

> Received 25/09/2026, 15:50:31 (Europe/London) from the CTO on the direct channel. Verbatim, taken from the session transcript, not retyped. Redactions per CONSTRAINTS.md: none needed (no address, no reference, no person's data).

---

This is the CTO: ruling 093. #318's erasure half is CLEARED as written, on three conditions. The CI CLI stays on `latest`, deliberately. File this as 093. Nothing here is NOT-FOR-RELAY.

Assumes: #318 head a5a587d; files are brief-real-trip-countdown.test.tsx (+39/−18) and erasure_fk_interlock.test.sql (+8/−2); RLS live and Quality were pending when I read them. I read the SQL diff: plan(7) is unchanged; 'c' is added for auth.mfa_recovery_code_sets.user_id, and the named 'n' class for auth.scim_users.user_id. If any of that has moved, say so first.

§1 CLASSIFY; DON'T PIN. Your reasoning is accepted.
- CI's `latest` CLI is the only thing that noticed GoTrue adding two tables that reference auth.users. Pinning it would keep CI green while the hosted auth, which Supabase upgrades for us, moved on.
- So `latest` stays on purpose. It's our sensor for auth-schema drift, and a red from it is information, not noise.
- This overrides the process audit's "pin the CLI" item. I'll strike that item on the plan page.

§2 THE CONDITIONS, before merge.
- (a) RLS live and Quality are green on a5a587d, with their run IDs in the PR body.
- (b) The body carries "## CTO clearance / ruling: 093".
- (c) The 'n' class rests on an absence: "SCIM is not configured, so no row exists". Pin that absence per 076 §1. Add one row to docs/pinned-absences.md. It is a row in an existing register, so the freeze allows it. The row states:
  - the claim (no auth.scim_users row can exist: SCIM/SSO provisioning is not configured on the project);
  - how it was established (say plainly that it comes from the configuration, not from reading the table);
  - what ends it (any plan change, or any auth provider/SSO/SCIM set-up);
  - what that obliges: the erasure runbook gains a step deleting the person's auth.scim_users row, and the 'n' classification is re-ruled.
  It goes in #318, or in a docs PR merged before #318; your call.

§3 THE RUNBOOK LINE: add it when SCIM is first configured, not now, as you recommended. §2(c)'s row is what makes "later" a deadline rather than a hope.

§4 THE JEST HALF: fine as built. It is not red lane. You named the previous form's run (e3b298a, 24 Sep 22:41), and the three mutants attribute the reds.

§5 ORDER, unchanged:
1. #318 (on §2);
2. #317 via merge-one.sh;
3. the chain per 087 §1, with a report after each merge.

PARKED: Francesca has build 19's five checks already; I'll batch any reminder.

---

## Correction to 093 §1 — received 25/09/2026, 15:50:41 (Europe/London), filed beside the ruling at the CTO's instruction. Verbatim.

This is the CTO, with a correction to 093 §1. File it beside 093.

I wrote "I'll strike that item on the plan page". I checked the page: it has no CLI-pinning item. The "pin the CLI" line was only in the backend audit's working notes, so there's nothing on the page to strike. The ruling itself stands.
