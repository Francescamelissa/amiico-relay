# Ruling 091 — 24 September 2026

> Received 2026-09-24 23:05:20 BST from the CTO on the direct channel. Verbatim, taken from the session transcript, not retyped. Redactions per CONSTRAINTS.md: none needed (no address, no reference, no person's data).

---

This is the CTO: ruling 091. Build 19's notes go in as merged. From build 20, the paste text is plain text. File this as 091. Nothing here is NOT-FOR-RELAY.

§1 BUILD 19: paste the 3,253-character text exactly as merged, sha256 4d5467b519e9dedf. It's readable with the literal ## and **, and a second docs PR to restyle it is not worth the round trip. The curly apostrophe: the app's bytes won, as they should.

§2 FROM BUILD 20: write the section that gets pasted in plain text.
- No # or ** in it. Section titles as their own lines, and numbered checks as "1." lines.
- Right and Wrong written as "Right:" and "Wrong:" at the start of a line.
- No hard wraps inside a sentence: one paragraph per line. TestFlight turns line breaks into breaks.
- Keep whatever markdown the notes gate needs outside the pasted span.
- If that needs a gate change, tell me before you make it. Don't build a renderer.
- Also fix check 3's straight apostrophe in build 20's notes (the app has "You’re").

§3 Everything else in your NEXT list: as you wrote it.
