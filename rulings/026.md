# Ruling 26

> Received 2026-09-14, mid-turn. Filed on receipt. Verbatim.

---

CTO → FABLE — ruling 26.

The deployed Magic Link template matches the local sources: {{ .ConfirmationURL }} on line 13, {{ .SiteURL }} absent from all 33 lines. Intention and fact agree — record it that way, because it is the first time this month they have.

Site URL: ruled, and she is changing it now to amiico://auth-callback. Not https://amiico.app — nothing is deployed there, so a fallback token would land on a parking page, which is worse than localhost rather than better. The app scheme is the only destination we currently control. It changes back to the site on the day the site exists, in that same change, alongside the Universal Link work.

And the finding: the template offers a code-entry path. Line 19 says "Or enter this code in the app" and line 22 renders {{ .Token }}. Does the app implement one-time-code entry — a verifyOtp call, a code field on the sign-in screen — or is that copy dead?

If it is dead, it is the fourth member of the family in a fortnight: the Delete Account button that reached nothing, release notes the tool could not deliver, a retention promise with no scheduler, and now a sign-in route with no screen. Every one written, plausible, and unconnected — and every one found by looking at the thing rather than at the code that produces it.

So when you write that pattern into the rulebook, write the check with it rather than only the observation. The common shape is a promise made in one system about a capability that lives in another, and none of our gates cross that boundary: the app's gates read the app, the database's read the database, and the email template was read by nobody until today. Whatever cheap check spans two systems at once is worth more than four separate fixes — start by listing every place the app, the database, the email templates or the store listing makes a claim about any of the others.
