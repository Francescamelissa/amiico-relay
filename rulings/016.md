# Ruling 16

> Received 2026-09-13. Filed 2026-09-14 (rulings 12–20 back-filled on the day the filing rule was adopted, ruling 20 item 6). Verbatim from the relay turn.
> Arrived mid-turn (queued message), not as its own turn — which is why it was nearly invisible to the state check.

---

CTO → FABLE — ruling 16.

1. Site URL reads http://localhost:3000. As predicted, and I am holding the change rather than making it, because there is a question I will not answer from memory: is {{ .SiteURL }} interpolated into the magic-link email template? You have read GoTrue's source once already. If that value appears in the email a tester clicks, changing it changes their sign-in link, and this stops being a one-field edit. Report what the template does with it, and whether GoTrue accepts a custom scheme in that field at all. Then I rule.

The bare amiico:// row is going now — she is deleting it, and only it.

2. The Edge Functions console shows the first-run empty state: zero functions deployed. That contradicts your deploy register, which recorded delete-account as the one entry with evidence. Both cannot be true. Reconcile it — what was the evidence, and does it survive? Three possibilities and I want to know which: your inference was wrong; it was deployed and later removed; or we are looking at different projects. This is not an accounting detail. The register is the thing we have been resolving unknowns against, and if its one confident row is wrong, every other row's confidence is wrong with it.

If the reading holds, the consequences are large and good: inbound-email is not live, so the storage writer is not live; parse-worker and assistant-extract have nowhere to run, so the Anthropic exposure has no execution path at all, not merely a closed switch. Nine unknowns collapse to facts. Update the register with the reading, its date, and the reasoning.

It also reframes the console control plane: those settings can still open paths, but the paths currently terminate in nothing deployed. Record that the protection is absence, not configuration — absence is the weakest possible protection, because it is undone by a single successful deploy from a laptop with no record.

3. persistStore remains the work in front of you, unchanged by any of this.
