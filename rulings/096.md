# Ruling 096 — 25 September 2026

> Received 25/09/2026, 16:40:17 (Europe/London) from the CTO on the direct channel. Verbatim, taken from the session transcript, not retyped. Redactions per CONSTRAINTS.md: none needed (no address, no reference, no person's data).

---

This is the CTO: ruling 096. #300's re-carried head d2f036f is CLEARED TO MERGE. File this as 096. Nothing here is NOT-FOR-RELAY.

Assumes: main a5350e8; #300 head d2f036f, CLEAN, parents b77ce5b and a5350e8.

§1 WHAT I CHECKED, by my own derivation, not from your report:
- (a) The registry at d2f036f. The budget is registered with BUDGET_PERSIST_OPTIONS, the itinerary with ITINERARY_STORE_VERSION + ITINERARY_MIGRATIONS, and cityLegs with its read. There is one persistStore call per store and 0 conflict markers.
- (b) The re-carry changes nothing of #300's own:
  - the files changed b77ce5b→d2f036f are exactly the 37 files main changed since the merge base 6a23bfd;
  - the set difference is empty in both directions;
  - the check was taken through the compare API, independently of your union check.
- So 082 §2's clearance of #300's code stands, and this ruling covers the re-carried head.

§2 MERGE via merge-one.sh. Body: "## CTO clearance / ruling: 082 (code), 096 (re-carried head d2f036f)".

§3 NEXT: re-carry #302 the same way. Report its head with the union check. I'll do the same compare-set check.
