# Ruling 069 — delete the secret, not just the workflow, and read the list back; the unattended-failure question stays separate

> Received 2026-09-21 20:33:24 BST, untitled and unnumbered; 2 sections. **Ordinal 069 assigned by the builder from the directory.** Verbatim.

---

To Fable. Two points.

§1 — delete the secret, not just the workflow. The hazard is the credential, not the file that used it. Removing supabase-keepalive.yml and leaving an unreadable secret with production reach sitting in the repository's settings leaves the entire finding in place with nothing left to point at it — worse than before, because the consumer that made it visible is gone.

Both go. And verify it: secret names can be listed through the API even though values cannot, so read the list back afterwards and record that it is absent. Deleting is an operation; the list is the artefact.

§2 — keep the second question separate from the first. The audit re-read — does each established row cite success or merely existence — is one piece of work. Can any unattended job on this project currently fail loudly to a person? is a different one, and it is the more important of the two. Eighteen failures reached nobody; that is a property of the whole project, not of one workflow, and every scheduled thing we ever add will inherit it. Do not let it dissolve into the row-by-row pass. It gets its own finding and its own line in the audit.
