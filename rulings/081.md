# Ruling 081 — the overnight brief of 22 September: the row-count reading (29/29, names and integers only); the query-to-a-human rule; Resend read and TLS enforced; the erasure resolved; eight users, seven identities; the zero pinned; the two columns dead; hostile fixtures; #303's body; the counts and the notice

> Received 2026-09-22 21:13:14 BST, untitled and unnumbered; 10 section(s) and a parked-list instruction. **Ordinal 081 assigned by the builder from the directory.** Verbatim. **Two copies arrived:** the first cut off mid-sentence in §3 at "and whether erase_user_admin, as it stands today," — a truncated relay with no manifest to show it; the second, minutes later, complete. This file is the complete copy.

---

AMIICO — OVERNIGHT PROMPT, night of 22 September. From the CTO, relayed by
Francesca, who is asleep. Nothing in here needs her. Park anything that does.

THE STANDING LINES.

- RED LANE = money · RLS · auth · migrations · a new sync entity · security ·
  personal data · any new external service · any new or upgraded native module.
  Design it, write it, test it, open the PR. Do not merge it. Silence is never
  approval.
- A merge to main IS a deployment; the Supabase GitHub App applies migrations
  from main within a minute. Every migration PR carries client compatibility,
  what happens to the build already in her hands, and a rollback.
- No force-push. No history rewrite. No --no-verify, no substitute git, no
  core.hooksPath, no HUSKY=0. Branch protection is never disabled to unblock a
  merge.
- Never service_role in the client, in EAS, or in the repo. No secret value
  enters a chat, ever.
- No person's data other than Francesca's enters a chat. Counts, not records.
  Never the contents of a sign-in email — the token IS the account.
- Money in integer minor units, display through formatMinor, exact-string
  assertions, toContain banned.
- npm ci first on every composite. Gates report their denominator. Floor the
  last number. Screens by NAME. The composite that gates a build runs alone,
  timed.
- Anything that must reach a subagent lives in the repository.

§0 — TONIGHT'S ARTEFACTS. FILE THESE FIRST; EVERYTHING ELSE DEPENDS ON THEM.

The row-count reading, run by Francesca in the Supabase SQL editor on the
night of 22 September 2026, self-verifying, complete (29 declared, 29
returned). Verbatim:

  ROWS IN THIS RESULT              29
  activity_log                      0
  assistant_usage                   0
  budgets                           0
  city_legs                         0
  email_parse_log                   0
  emergency_contacts                0
  entitlements                      0
  expense_splits                    0
  expenses                          0
  inbound_emails                    0
  itinerary_days                    0
  itinerary_items                   0
  job_runs                          0
  morning_briefs                    0
  profiles                          8
  settlements                       0
  storage_purge_queue               0
  subscriptions                     0
  trip_inbound_addresses            0
  trip_invites                      0
  trip_members                      3
  trips                             3
  wallet_items                      0
  expenses.converted_amount not null  0
  expenses.fx_rate not null           0
  auth.audit_log_entries            0
  auth.identities                   7
  auth.users                        8

Note the shape of the query, because it is the new standard and it is why you
are reading this instead of a screenshot: it returns only names I typed and
integers. It is INCAPABLE of returning a person's identifier. That property is
structural, not a matter of anyone being careful, and it exists because this
morning I sent Francesca a query that could return email addresses with an
instruction not to paste it back, she reasonably pasted it back, and one real
person's address left the database as a result. Logged as an incident on my
side. File the rule where the probes rule lives: A QUERY SENT TO A HUMAN IS
WRITTEN SO THAT IT CANNOT RETURN PERSONAL DATA; AN INSTRUCTION NOT TO PASTE IT
IS NOT A CONTROL.

Also file, dated 22 September: Resend open tracking OFF, click tracking OFF,
webhooks list EMPTY — all three read from artefacts, not settings text. And
Resend TLS moved from Opportunistic to ENFORCED on the amiico.app domain, on
my ruling, because every email Amiico sends is an authentication email
carrying a credential and Opportunistic falls back to plaintext silently.
Resend's own guidance names authentication emails as the Enforced case. Record
the reason with the change; this is the kind of setting someone reverts in six
months because nobody wrote down why.

§1 — BUILD 19. STILL FIRST.

Row 72's artefact reached you this morning. If the gates are green and the cut
has not happened, cut it: composite alone and timed, the bundle proof against
build 18's control (expect 0 · 0 · 0 · 1), one submission, no re-cut. If the
gates are still red, report the red verbatim with its denominator and move on
— do not work around it.

§2 — THE ERASURE IS RESOLVED. CLOSE IT PROPERLY.

The subject searched his own inbox and confirmed there is no Amiico email and
no account was ever created. That matches the database exactly: no account
under the address he gave, trip_invites 0, emergency_contacts 0. There is
nothing to erase. A closing letter goes out confirming it, naming what is
retained and pointing at the ICO.

docs/erasure-rehearsal.md gains the case it did not have — A REQUEST THAT
MATCHES NO ACCOUNT — as a worked example with the real dates: request 14 Sep;
identity query 22 Sep returning 0 exact / 1 similar / 8 total; first
clarification; a reply restating the same address; second clarification
pointing at the artefact rather than memory; resolution. Two rules come out of
it and belong at the top of that file:

  A resemblance is not an identification, and the cost of acting on one is
  permanent.
  Where a subject cannot be identified, the deliverable is a documented
  refusal, not a best guess.

And a residual the list has never carried: THE RECORD OF THE REQUEST ITSELF.
The correspondence is personal data retained deliberately, for two years, to
demonstrate compliance. It survives every erasure by design. The privacy
notice has to say so, and the runbook has to stop implying the file is empty
when it is done.

#299's two decisions, both ruled this morning: message_id becomes
'erased:' || id; block E stays hand-run for the first real erasure and moves
into private.erase_user immediately afterwards, as a dated commitment written
into the runbook now. Still red lane, still held. One outstanding: the delete
action on inbound_emails.trip_id, cited from pg_constraint.

§3 — EIGHT USERS, SEVEN IDENTITIES. THE SHARP ONE.

auth.users is 8 and auth.identities is 7, and one of the eight is
deleted-account@amiico.invalid — created 21 July, never signed in, its address
replaced with a placeholder. That pattern says an earlier deletion RENAMED an
account rather than removing it, and dropped its identity while keeping the
user row.

Establish, from artefacts: what created that row and when; whether any code
path — an RPC, an Edge Function, a migration, a script — updates
auth.users.email rather than deleting the row; and whether erase_user_admin,
as it stands today, deletes or anonymises. If it anonymises, then the residual
list in the runbook is wrong in a way that matters, because we have been
telling ourselves an erasure removes the account.

Identify which of the eight has no identity. Counts and a boolean are enough —
do not put an address in a report.

§4 — PIN THE ZERO.

auth.audit_log_entries came back 0, which falsifies your 6 September
prediction that the probe left audit rows. Good — a prediction tested. But a
zero count is not yet a reading: it is equally consistent with "no rows" and
"this role cannot see the rows." Establish which, from the catalog — whether
the table has RLS enabled, whether GoTrue on this project writes audit entries
at all, and what the retention is. Then the probe register's P1 row gets its
predicted-versus-actual column filled in properly, including the one that was
wrong.

While you are there: the probe DID create an auth.users row, unconfirmed,
6 September 18:44:39 — one row, not three, despite three sends. That is now
known rather than likely. Update P1.

§5 — THE TWO COLUMNS ARE DEAD, AND THE DROP WAITS.

expenses.converted_amount and expenses.fx_rate: zero rows have ever carried
either, and expenses itself is empty. Row 70 answered. Write the guarded drop
migration and open it HELD — but it does not merge until build 19 is in her
hands and confirmed working. A migration deploys on merge; we paid four days
for that lesson last week and we are not paying it twice while the shipped
client is already broken.

§6 — #300 AND #302: THE HOSTILE FIXTURES.

As ruled this morning. The build-18 snapshot models the third client; it
cannot, by construction, contain the record shape that is absent from it. Add
the totality set — empty store at v1; unknown extra field; null where the
snapshot has a value; empty array where it has entries; and for the wallet, a
record missing its projection fields entirely. Each with its own demonstrated
red. The migration must neither throw nor drop a record on any of them.

Then paste for the read: the snapshot's bytes and sha256 plus the generator's
commit and pinned configuration; the migration function's full text; the
assertions verbatim; the reds as run. Not the diff.

§7 — #303's BODY, VERBATIM.

Paste the four decisions as written and they come back ruled in one pass. B
over A is already decided — flat arrays with a tripId on the record, not a
persisted map, because a map makes "a record with no trip" unrepresentable and
that is exactly what is on her phone. Money arithmetic must not appear in the
budget diff at all.

§8 — WHAT THE COUNTS MEAN FOR THE NOTICE, AND ONE CLEANUP.

Four tables hold anything: profiles 8, trips 3, trip_members 3, auth.users 8.
Everything else is zero. Five of the eight accounts are our own test artefacts
(two smoke, two sim, one probe), one is a deletion tombstone. Every figure
either of us has quoted off "8 accounts" has been carrying six non-people.

Write the cleanup plan for the five test accounts and the tombstone — proposed
only, red lane, nothing executed, and it runs after build 19 and after the
erasure file is closed. State for each what it would take with it.

And give me one line I can put in the privacy notice, established from these
counts rather than from belief: what the server actually holds today.

§9 — REPORTING, AND DO NOT STOP.

Manifest at the top with the section numbers received. One block per section,
in order, including the boring ones. Commands for every absence. Corrections
to anything you told me earlier, plainly, at the end.

Anything needing Francesca: one PARKED list, five lines maximum, at the very
bottom. She has done four readings today and sent two letters; the bar for
adding to her list tonight is high.

When §8 is done, go back to the keying order — itinerary and wallet are open
for read, budget and brief wait on #303's decisions. Open PRs, hold the red
lane ones, keep going.
