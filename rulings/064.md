# Ruling 064 — the day list: standing rules (no migration merges; park decisions), eleven items in order, what is not the builder's to decide, and the report format

> Received 2026-09-21 07:38:43 BST, untitled and unnumbered; 4 sections. **Ordinal 064 assigned by the builder from the directory.** Verbatim.

---

A full day. Work top-down; do not skip. The hold is lifted.

§A — standing rules

Committed increments — never end a unit with uncommitted tracked changes. Every worktree add through the scratch-tree helper. Every validator call || exit 1; a failure is obeyed, not noticed. All writes through the write helper. If the composite goes red and you cannot make it green, stop and write up rather than stacking on it.

No migration merges today. A migration merge is a production deploy — that is the finding of the week — and a deploy needs an authorisation I have not given. Nothing in items 1–11 requires one. If something turns out to, park it.

And when a decision needs me, park it with the question stated and move to the next item. A day built on a guess I would have refused is worse than a day with one item parked.

§B — the order of work

1. Merge #272 and #273. Both cleared. Neither is a migration. Composite green on main afterwards, alone, toolchain recorded.

2. File both readings. The migrations list and the three schema counts. If her paste has not reached you, record what I sighted — both new versions present in schema_migrations; trips.currency = 1, trips.home_currency = 0, profiles.home_currency = 0 — marked as CTO-sighted, pending her paste, because my transcription is evidence of a reading and not the reading. Ask her once. main's migrations-live red clears on this, for the first time since the gate existed.

3. The client–schema correspondence, by hand, for build 19. Enumerate every column build 19 will name — reads and writes — and compare each against the live reading. Report the comparison as a table before anything is cut. This is the check that would have caught the incident on the 19th and it is worth doing slowly.

4. Build the gate behind it. Item 3, automated: reds when the build names a column the live reading does not carry, or when the reading is missing or stale. In the composite and first in submit-build.sh, beside the migrations gate. Demonstrated red from a fabricated reading, not from a damaged repository.

5. The fallback message. "Couldn't reach your trips just now" named the wrong cause and sent the only user to check a wifi that was fine, for four days. Separate network unavailable — normal, quiet, expected on a trip — from the server rejected our query, which is an incident. Different wording, and the second has to reach us rather than only her. A fallback that looks like success hides the failure it exists for.

6. Build 19. Cut from main once 1–5 are green. Draft the release notes into a file in the repository first — that rule exists because the last set shipped from a chat message and neither of us has them. Verify the IPA before submission exactly as for build 18. Hold the submission until I have read item 3's comparison and the notes; everything up to that point is yours.

7. The mechanism audit — the root-cause response, and the item most likely to find the next one. Migrations had a deploy path documented in this repository that neither of us was holding in mind. Ask the same question of everything else: what builds the app, what deploys Edge Functions, what applies storage policies, what publishes the website, what changes the mail templates, what enforces branch protection. For each: established, with the artefact that establishes it — or assumed. Assumed is a finding, not a gap to fill quietly.

8. Supabase Preview as a required check on main. If merging deploys, the deploy check gates the merge. Before-reading, the change, after-reading, all from the API, and a row in the inventory. Adding a protection is the safe direction and it is cleared.

9. The red-lane checklist gains deployment conditions. Client compatibility, what happens to the build already in people's hands, and the rollback. Mine to answer for each migration; yours to write into the checklist so I cannot clear one without being asked.

10. Activity keying — implementation. Released. Design and failing tests are on main; build it. PR left open for my read, not merged — it is the change that nearly deleted every city she has entered.

11. Row 35. The founder's own surname as an Apple sign-in fixture in social.test.ts:280. Same treatment as the seed persona: a name with no connection to anyone real.

§C — not yours to decide today

The release-notes text, build 19's submission, anything that writes to production data, the erasure in any form, any new external service, and any migration merge. Park, state the question, move on.

§D — the report

Manifest first. Then item by item: the done-condition and the evidence that met it. Then what was parked and the exact question each waits on. Then the composite's final state — gate count, reds, timing, toolchain. Then item 7's audit table in full, because that is the one I most want to read.
