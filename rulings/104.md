# Ruling 104 — 25 September 2026

> Received 25/09/2026, 18:38:50 (Europe/London) from the CTO on the direct channel. Verbatim, taken from the session transcript, not retyped. Redactions per CONSTRAINTS.md: none needed (no address, no reference, no person's data).

---

This is the CTO: ruling 104. #323 is CLEARED, with (d) folded in before it merges. File this as 104. Nothing here is NOT-FOR-RELAY.

Assumes: main d748537; #323 head ee7edef (3 files); CI still refused.

§1 WHAT I READ: the ci.yml diff.
- The push trigger is gone, replaced by a Monday 06:00 UTC schedule plus workflow_dispatch. The reasoning is in the file, and it's correct given strict=true.
- Concurrency is exactly as ruled.
- Each job's scope step names the code its skipped steps read, and a doc named by that code forces the full path. That's the 38-of-77 finding handled at the right level.
- Your measurement was complete rather than sampled: 510 runs, and the replay closes to the minute. Better than my estimate.

§2 (d): YES. Fold it into #323 before merge; don't open another PR, which would cost another run.
- RLS live takes its 1-minute path when a code PR touches none of its inputs: supabase/**, the RLS, smoke and reading scripts, docs/rls-live-readings.md, ci.yml, package.json and package-lock.json.
- Reason: pgTAP re-proving an unchanged schema and unchanged tests on an app-only PR produces the same answer every time. What only time changes is covered by the weekly run.
- Add one mutant: an input missing from the list. A PR touching it must still take the full path.

§3 THE TARGET: accepted as unreachable by trimming at September's pace.
- September's pace was the process spike. Most of those PRs were ledger, queue and readings documents, which the Phase 1 reset retires.
- Your part of the pace, as you offered: iterate locally, push once, fewer and larger PRs.
- The rest is Francesca's spending limit or plan, which I'm handling.

§4 ORDER WHEN CI RETURNS:
1. #323 (its own run is a full run);
2. #321;
3. #322;
4. build 20's live reading as a docs PR (docs-only for quality and RLS audit, full for RLS live, by your own scenario);
5. the cut.
Nothing merges before billing is resolved.

PARKED: nothing new.
