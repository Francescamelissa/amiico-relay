# Ruling 098 — 25 September 2026

> Received 25/09/2026, 17:13:20 (Europe/London) from the CTO on the direct channel. Verbatim, taken from the session transcript, not retyped. Redactions per CONSTRAINTS.md: none needed (no address, no reference, no person's data).

---

This is the CTO: ruling 098. #309 at head 8ea4438 is CLEARED TO MERGE. It is money, cleared on 086 §3 (a)–(f), all met, and on my own read of the head. File this as 098. Nothing here is NOT-FOR-RELAY.

Assumes: main 417d0db; #309 head 8ea4438, CLEAN; CI run 36158530977 4/4.

§1 WHAT I VERIFIED MYSELF, from the artefacts at 8ea4438:
- The blob SHAs are identical at the head and on main:
  - money.test.ts b2521c4;
  - state.test.ts 1768b6b;
  - money.ts 05ecab3;
  - conversion.ts 7b7f745.
  So the withdrawal conditions are not triggered.
- purgeTripBudget (state.ts:496) filters budgets, expenses, settlements and alerts by tripId. deleteTrip calls it (tripLifecycle.ts:264). No `budgetStore.set(EMPTY_BUDGET_STATE)` remains in tripLifecycle.ts.
- adoptUnscopedBudget (:479) re-stamps only BUDGET_UNSCOPED_TRIP_ID rows, and is a no-op when there are none. It is wired in the boot adoption block, the same as itinerary and wallet (registerPersistence.ts:447ff).
- budgetForTrip (:432) filters every collection by tripId before the byte-identical money functions see it. envelopeFromView (:361) writes back only this trip's rows and keeps every other trip's.

§2 THE CATCH OF THE RE-CARRY: dropping main's `budgetStore.set(EMPTY_BUDGET_STATE)` from deleteTrip.
- A union would have kept it, and it would have wiped every trip's money on deleting the current trip.
- It is exactly the class 068 says only a hand resolution can destroy, running the other way: a line that had to GO.
- The union check could not see it. Record that in the PR body as a limit of the check, not a defect of it.

§3 MERGE via merge-one.sh. Body:
"## CTO clearance / ruling: 086 (conditions a–f; f added by 095 §3), 098 (re-carried head 8ea4438)".

§4 NEXT, as you listed:
1. The read-guard PR for wallet, itinerary and `you` (095 §4), for my read.
2. #311's re-carry, plus the guard it lacks: with the lock gone, no TRIP_SCOPED_RESOURCES entry except currentTrip may be purged-when-current. Red on the old branch in a scratch tree, then green.
3. Then build 20, with its expect file derived from the tree, not from memory.
   - Build 20's notes are the first in plain text (091 §2).
   - The five checks must include creating a second trip, and adding an expense to trip B while trip A's total stays unchanged. That last one is the money control, done by her hand.
