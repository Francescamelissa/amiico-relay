# Ruling 087 — 24 September 2026

> Received 2026-09-24 19:50:43 BST from the CTO on the direct channel (confirmed by Francesca before 084–086 were filed). Verbatim, taken from the session transcript, not retyped. Redactions per CONSTRAINTS.md: none needed (no address, no reference, no person's data).

---

This is the CTO: ruling 087. The keying chain goes before B1–B5. The merge route is accepted. Your ten Bible corrections are accepted and I'm fixing the page. File this as 087. Nothing here is NOT-FOR-RELAY.

Assumes: main = 5842ef8; build 19 not cut; the reading is with Francesca. If any of that has moved, say so before you act.

§1 ORDER. Your recommendation is accepted.
- After the build 19 cut: the chain first. #310 → F1 guard → #300 → #302 → #309 (with 086 §3's conditions) → #311 plus its guard.
- Then B1–B5 on the keyed tree. That saves re-carrying B1 and B3 over six PRs.
- Builds:
  - Cut build 20 as soon as the chain is in, since multi-trip is her top request.
  - Cut build 21 when B1–B5 are in.
  - One submission each, the full procedure each time.

§2 MERGE ROUTE (queue row 60): closed. merge-one.sh, the way you used it, is the route: squash, all required checks, CLEAN only, clearance gate, body carries the ruling. merge-gate.sh goes on the Phase 1 retirement list.

§3 B3 REVISED. You are right that sign-out already clears saved places in memory (session.ts:189) and on disk (clearAllPersistedStores). The sign-out part becomes a test that goes red if clearSavedPlaces() is removed. The remaining work is unchanged:
- delete-trip purges the trip's saved places;
- the export includes them;
- PERSISTED_STORE_KEYS is derived from the registry.

§4 B1 SCOPE, confirmed.
- The in-memory reset list: you, budget, wallet, itinerary, brief, activity, tripConversion. Plus the confirmation step.
- The proof: sign out, then sign in as a second test account WITHOUT restarting the app. Nothing of the first account shows.

§5 THE CRYPTO LINE. My page was ambiguous, and you read it the other way. It meant SQLCipher: package.json's op-sqlite `"sqlcipher": true`, plus @op-engineering/op-sqlite and @powersync/op-sqlite, which only unreachable code requires.
- expo-crypto stays and is used by B2.
- My understanding is that randomUUID uses the OS's own crypto and does not change the export-compliance answer. That is not established. The IPA read in Phase 4 settles it.
- The Phase 1 removal item is SQLCipher/op-sqlite, not expo-crypto.

§6 THE REST OF §7. All accepted. I'll strike through the wrong lines on the page, not rewrite them:
- items 1 and 2: the saved places and sign-out claims;
- the email code line on the builder's list;
- 4 sites, not 3;
- BUILD-SPEC scoped: 57 in app code, 82 across the tree;
- census: 3 counted, plus /budget reachable only as a back-button fallback;
- 72 as the migrations' and CI's count, production unread;
- the chain now includes #310 and the F1 guard.
Your git grep correction is noted.

PARKED (Francesca): the reading's CSV and the Plan line, as you've already asked.
