# The queue — one file, worked top-down (ruling 034 §2, 039 §2)

Priorities are not issued in prose. Each row carries who raised it, what
closes it stated as an **observable** (a file, a number, a read-back — never
an intention), and its state: **ready** · **in progress** · **held by CTO**
(waiting on a ruling or on text reaching the CTO) · **held by founder**
(waiting on an action only she can take) · **done** (with the observable that
closed it). The builder edits this file; the CTO amends by text and the file
decides (039 §2). Rows are worked from the top. A row that cannot be worked is
moved, not skipped silently.

| # | item | raised by | closes when (observable) | state |
| --- | --- | --- | --- | --- |
| 1 | The taint window: every reading, count and gate result recorded between 20:24:45 and the toolchain being set is enumerated and re-taken, with exit status asserted and a positive control beside each gh reading | CTO (040 §1, §3) | the enumeration is in the builder's reply with each row marked re-taken or superseded; `scripts/lib/gh-read.js` is on `main` | **done** — reply of 2026-09-15 21:05; PR #254 |
| 2 | Empty-corpus check: did any composite gate pass inside the window on an input set it never had | CTO (040 §2) | each gate that enumerates through git/gh is named with what it did on the night; every counting gate asserts a floor | **done** — reply of 21:05; `rulings-sequence`, `unseen-branches`, `tap-target-audit` reds shown; PR #254 |
| 3 | The three `git reset --hard` losses: recovered work verified against the pre-reset copy and the transcript; the probe procedure moved to a second tree | builder (found 20:52) | the five files' content matches copy + replayed edits (composite 18/0); the rule is in ENGINEERING parent 4 with the instance | **done** — merged in #254 (`bfa5bbf`); attributed as the CTO's design (042 §1); the two causes partitioned (042 §2) · the scratch-tree helper is row 20 |
| 4 | PR #254 review | builder | merged, or returned with sections | **done** — #254 merged; #250 merged `f8f11de` and #251 re-carried as #260 merged `2e3ba59` (054 §1, conditions met 2026-09-16) |
| 5 | Erasure runbook `docs/erasure-rehearsal.md` (#252): point of no return marked; trips rows captured before (ids + owner ids saved durably outside the repo); trips prediction before N / after N−1 or N; "not exercised" for zero-before rows; entity_id row 41 + erase statement; audit-log delete step 6; invited_email erase; then its TEXT pasted to the CTO | CTO (029 §2–§4, 031 §B–§D) | the CTO has the runbook text in a message (they cannot read repo paths); #250/#251 unblock on it | **done** — the runbook is on `main` (#252 `d9a558b`), every ruled addition in it; the erasure itself waits on the founder's dress rehearsal (step 0) and the before-counts |
| 6 | Article 12(3) erasure of USER-B, due 2026-10-14 | CTO (031 §A) | the after-state query returns the predicted counts; residuals listed; the subject's identifiers never in rulings/, runbook, commit or fixture | **held by CTO** — on row 5's runbook being read |
| 7 | Ruling 029 §2: `activity_log.entity_id` can hold a user id — query row 41 and an erase statement (migration text, red lane) | CTO (029 §2) | the migration text is in a PR body for red-lane review; the runbook's row count is read from the file | **done — APPLIED TO PRODUCTION 2026-09-19 10:00 BST by the Supabase GitHub App on #257's merge** (061; the builder's "not yet applied" was wrong — correction entry in the ledger, #273); the schema reading is the artefact, awaited from the founder |
| 8 | Ruling 029 §3: the FK map derived from the catalog (pgTAP), not regex over migrations | CTO (029 §3) | a pgTAP assertion listing the 21 FK columns from `pg_constraint` is green, and a red is shown by dropping one in a copy | **CLOSED (080 §5)** on `supabase/tests/erasure_fk_interlock.test.sql` |
| 9 | Ruling 029 §4 / 030 §D: CI seeded cascade fixture — one row in every place a user id can live, including an `activity_log` row per `entity_type` value | CTO | the before-state query on the fixture returns non-zero on every row that can be non-zero; "not exercised" rows are named | **done** — #257 open (`c8f8643`): 28 places seeded, both stages, four residuals asserted to survive, plan(45) green in CI; red via CI by removing one cascade statement (test 16, and the auth delete BLOCKED — a missing re-point stops stage 2) |
| 10 | Ruling 029 §5: residuals — audit-log delete (self-asserting count/delete/count/raise, privilege check), `trip_invites.invited_email` erase, summaries must not store names as prose (or the DPIA comment corrected) | CTO | each residual has either an erase step in the runbook or a sentence saying why it survives, and the DPIA comment matches the code | **CLOSED on two clauses (080 §5)**; the third is a deadline — the pin in `pinned-absences.test.js` reds the day `logActivity` gains a caller without a summary-content test |
| 11 | Ruling 029 §6: trip survival — what writes trips to the server (`create-trip.tsx` insert), the delete path (`tripLifecycle`), the RLS delete policy on `trips` | CTO | three file:line citations in a reply, each read from the tree | **done** (051 §B.5) — WRITES: `app/(onboarding)/create-trip.tsx:258` `supabase.from('trips').insert(...)` inside `handleCreate` (:218), fired by the Create button (:524), gated on a client and a session — a DIRECT Supabase write, not the sync engine. The only other direct writes in the app are on the same table: `tripLifecycle.ts:137` (cancel: update cancelled_at) and `:186` (delete). WHY TRIPS ALONE: every child (wallet, itinerary, cityLegs, budget, activity, you) goes through `authorMutation` into `fakeEngine.ts`'s outbox, which never flushes — the PowerSync adapter does not exist; the engine's "no network" comment is true. So the children are UNBUILT, not broken: their server path is a component nobody has written. DELETE: `deleteTrip` runs `from('trips').delete().eq('id')` (RLS: `trips: only owner can delete`, `20260702220200:137`; 15 child FKs cascade on the server), then purges locally (trips list, presentation, cover file; the four un-keyed stores only if the trip was current) and flushes |
| 12 | Ruling 029 §7: the code-entry contradiction — the app has NO code entry; the dead copy is in the template, not the app | builder (returning the contradiction) | the CTO rules which side changes | **held by CTO** |
| 13 | Ledger entries: hook not a control; `HUSKY=0` observed both arms; ruleset before 0 / after 1; the toolchain incident (shim set: git, python3, clang, make, swift); the three resets; the snapshot and its note | CTO (037 §3), builder | the lines are in `docs/MASTER-LEDGER.md` on `main` | **done** — on `main` at `bfa5bbf` |
| 14 | Build-19 notes amendment (PDF / itinerary / expenses checks); build 18's `## What shipped` recovered verbatim from App Store Connect | CTO (029 §8) | the record block is present under `## What shipped` with the paste comment; the notes gate is green | **held by founder** — the ASC text must be pasted by her |
| 15 | Activity keying: `ActivityEntry` has no `tripId` — the full §4 migration | CTO (023) | activity entries are keyed by trip in memory (the store is not persisted), with the lock's protection proven by a test that reds when two trips share a list | **held by CTO** — design + `it.failing` tests on `main` (#259 `3a8486a`); implementation is the next PR when you say |
| 16 | The lost six-section reply, resent in parts each naming its sections | CTO (033) | the CTO confirms receipt of every section by name | **in progress** — superseded in substance by 040/041 and this reply; the parts are sent with this reply's manifest |
| 17 | `unseen-branches` reads 165 remote-tracking refs while the API lists 120 branches — 45 stale refs on this machine | builder (found 21:00) | `git remote prune origin` run and the two counts agree, or the difference is explained by name | **done** — 45 pruned; 123 refs = 122 API branches + HEAD; unseen-branches reads 122 |
| 18 | The edit helper reports `bytesBefore/After` as character counts (UTF-8 multibyte differs); rename or count bytes | builder | the field name matches what it measures; the fixture still passes | **done** — #291 (bytes via Buffer.byteLength; a multibyte case the old counting fails) |
| 19 | Founder: `sudo xcodebuild -license accept` (lifts the shim taint for every tool); export run on build 18; Resend page readings (#20/#21) | builder / CTO | the composite's toolchain block reads `git --version` succeeding with `DEVELOPER_DIR` unset (the `~/.zshenv` block is self-retiring: it exports nothing once `/usr/bin/git` runs) | **held by founder** — meanwhile `DEVELOPER_DIR` is inherited by the builder's shells (042 §3, a decision) |
| 20 | A scratch-tree helper for probes: `git worktree add` under the scratchpad, the probe runs there, the main tree never moves (042 §1) | CTO (042 §1) | `scripts/lib/scratch-tree.sh` exists, the hook probe is re-run through it with the main tree's mtimes unchanged before/after, and it is the only probe path named in ENGINEERING | **done** — #256 open (`63c6ccd`): helper + fixture (5 cases; control; red by bypass); shellcheck and the pipeline lint cover scripts/lib |
| 21 | The sweep "whom is this row about" — the free-text column (expenses, settlements, itinerary notes, trip names, summaries, brief payloads) cannot be counted without contents; a DPIA item, not a runbook row | CTO (045 §1) | the DPIA lists each free-text column with its retention and the notice's sentence covers text about you in other people's records | **ready** |
| 22 | The export PDF: header carries the trip's name and dates or is silently omitting them; the generator on a trip with content — read by the builder on this machine, from a trip with no third party's details in it | CTO (045 §2) | a PDF from the founder's new test trip is read here and its sections are named against the build-18 claim | **held by founder** — the new trip's export, copied to ~/Desktop |
| 23 | Export: a city whose GeoNames admin-1 region equals its own name prints twice ("Mexico City, Mexico City") — at SOURCE (727 of 33,936 catalog entries); render fix omits the region when it equals the name | CTO (046 §2) | the export test renders a leg with region === name and asserts a single name; her next export shows "Mexico City" once | **done** — landed 2026-09-16 (dc04e36: a region equal to the city is not printed); row was stale |
| 24 | Export: the trip date range and per-city dates carry no year (formatShortDate is a screen helper by design); an export needs a year-bearing formatter | CTO (046 §2) | the export test asserts the year on a range that crosses 31 Dec; her next export shows both years | **done** — landed 2026-09-16 (dc04e36: every date in the summary carries its year); row was stale |
| 25 | Export: whether the Budget/expenses section renders on a trip that has expenses (the build-18 notes told testers to look for it) | CTO (046 §1) | read from her populated trip's export on this machine, or from the generator with a fixture that has expenses | **held by founder** — the answer to "were expenses entered" |
| 26 | Export carries the account's emergency contact into every trip summary users are told to share — a deliberate decision to write down (notice + decisions.md), not a change tonight | CTO (046 §3) | a decisions.md entry and a notice sentence, both cleared | **done** — #262 `2a3fb9d` |
| 27 | trips.home_currency: the create-trip control is labelled "Home currency", defaults GBP, is user-picked; the app uses it as the trip's SPENDING currency (default for budgets and expenses); the money migration treats it as the reporting currency — the name lies; rename or split before any conversion | CTO (047 §1) | a red-lane migration PR with the column meaning stated and the money comment corrected; the create-trip label matches | **done — APPLIED TO PRODUCTION 2026-09-19 10:54 BST by the Supabase GitHub App on #264's merge, with the 056 §1 precondition in the file; the guard ran before the drop and did not raise** (061); the schema reading (trips.currency present, both home_currency columns gone) is the artefact, awaited from the founder |
| 28 | Conversion v1 design (user-entered rate per trip, stored as a fact with its date; display-only; every figure prints rate + date) — red lane, held | CTO (047 §2) | an ADR, blind dual review | **held by CTO** — the shape and the deliberate removal of the home currency recorded in `docs/conversion-v1-design.md` (#263, 056 §2) |
| 29 | set-budget.tsx: the Push alerts toggle accepts input and nothing arrives (fifth instance); wire, disable with a line saying why, or remove | CTO (047 §3) | the toggle is gone or disabled with its reason in the file; a test reddens if it becomes enabled without the notifications feature | **done** — #261 `1d91dd2` |
| 30 | set-budget.tsx: APPLIES TO Whole trip / Just me cannot differ until companions exist — a note in the file, not an action | CTO (047 §3) | the comment is beside the control | **done** — the APPLIES TO note is in app/budget/set-budget.tsx (047 §3); row was stale |
| 31 | Verification of exported files moves to the builder's machine (045 §2) — but her account's real emergency contact prints in every export, so a PDF of hers carries a third party's name and number; the two rules collide | builder (returning the contradiction) | the CTO rules: a placeholder emergency contact on the tester account before exporting, or the CTO keeps reading exports | **ruled** (052 §2): content verification moves to seeded test data; her real account only proves the mechanism; the builder reads that file |
| 32 | `supabase/seed.sql:105` seeds an emergency contact named "Anna Caristo" with a fictional-range phone — a real-looking relative's name in a public repo's seed data; the seed-data gate hunts BUILD-SPEC literals, not names | builder (found while seeding the cascade fixture) | the seed row carries an obviously fictional name, or the CTO rules the name is fictional | **done** — #265 `2b97dec` |
| 33 | The cascade fixture's red showed a missing re-point BLOCKS the auth delete (`wallet_items.added_by → profiles` is restrict) — the runbook now says step 2 must succeed before step 3; worth a line in the notice's runbook comment in the function | builder | a comment beside (d) in `private.erase_user` naming which columns are restrict and why the order is load-bearing | **done** — `erasure_fk_interlock.test.sql` names all fifteen NO ACTION columns with why each is re-pointed, proves the refusal by exercising it (23503) and its release after erase_user |
| 34 | The write helper's staged rename stripped the executable bit from the first executable edited through it — fixed with a red (#266, merged `debb9e9`); a closed instance of "your own tooling is untested software" | builder | merged | **done** |
| 35 | `src/features/auth/__tests__/social.test.ts:280` uses the founder's own surname as an Apple sign-in name-parsing case — same pattern as row 32, her own name rather than a relative's | builder | renamed to a fixture surname, or ruled acceptable | **held by CTO** |
| 36 | The migrations live reading (057 §1): `docs/migrations-live-readings.md` + gate, in the composite and first in submit-build — the composite on main is RED on it until the first reading is pasted | CTO (057 §1) | the founder pastes the SQL editor's `schema_migrations` result under a dated, signed heading; the gate reads OK | **done — merged `461ef71` on 2026-09-19 under ruling 059 §1, REVERSING 058 §1's hold.** The hold rested on two assumptions: (a) that the composite might be a required check and merging a red would deadlock the repository — **false, read from the API (058 §3): the composite is not a CI job; the four required checks are the CI jobs** — and (b) that a red nobody present can clear erodes the signal — much weaker once the audience is known (hand-run at a cut by the builder and the CTO, not a stream of contributors). What remained: a false green on a fact nobody has checked is worse than a true red nobody is acting on. The composite on `main` is red on this gate — 44 migrations, no reading — until the founder's session (dress rehearsal + first reading), which now clears the red rather than preceding it |
| 37 | The drop guard seen to fail on purpose (057 §2): pgTAP re-creates the column, seeds EUR, asserts P0001; the guard text pinned to the migration by a jest lockstep | CTO (057 §2) | CI's pgTAP job green with the throws_ok | **done** — #268 `45bec94`; named in the ledger as the single most valuable red in the repository (058 §2) |
| 38 | The helper carries full metadata (mode, ownership, xattrs) and refuses hard links (057 §4) | CTO (057 §4) | fixture asserts each property; reds shown | **done** — #269 `eec7b16` |
| 39 | Validators in the builder's own commands halt mechanically on failure (057 §3) — the rule is in ENGINEERING; the 44033 row in the interpretation table | CTO (057 §3) | no ad-hoc command continues past a FAIL — observable only in the absence of the next instance | **in force** |
| 40 | The composite exits 0 on a red — by its own 2026-08-13 declaration ("The composite reports. It does not clear a cut — that is a human ruling"). Under 057 §3 that makes it a REPORT read by a person; since then it carries cut-blocking gates. Should it become a gate (non-zero exit on any red)? `submit-build.sh` already halts (set -e), so the cut is safe either way; the question is the composite's own exit | builder (found 2026-09-19 running the first true red) | the CTO rules; if a gate, `exit "$fails"` with a fixture red | **done pending merge** — ruled 060 §1: a gate, 0/1/3 with the count in the output; #272 with the fixture (five cases, red by restoring exit 0); also restores composite.sh's +x, stripped by #260 on the 16th |
| 41 | scratch-tree is the only route to a worktree: `--branch` mode, the obeyed failure, the lint shape; row 33 in the inventory (059 §2–§3) | CTO (059 §2) | merged | **done** — #271 `c8c4e59` |
| 42 | 061 §4 HOLD: no command that can write to a database runs — locally, in a test, in passing — until the mechanism is settled. Settled 2026-09-20: the Supabase GitHub App on merge (inventory row 17, established 2026-09-02); no builder command touched a database since 2026-07-03 (the last `supabase db push`, deliberate); the local CLI is broken and this Mac has no container runtime. The hold expires on the CTO's reading of #273's evidence | CTO (061 §4) | the CTO lifts it | **done** — lifted 2026-09-21 on the builder's reading (063 §4, 065 §1): no command of the builder's, the GitHub App on merge, timestamps from the API |
| 43 | 061 §2: the catalog reading that separates history from schema — `trips.currency` exists, `trips.home_currency` gone, `profiles.home_currency` gone — pasted into `docs/migrations-live-readings.md` beside the first migrations reading | CTO (061 §2) | the reading is in the file, dated and signed | **held by founder** — asked once 2026-09-21; one query fills this and row 48 |
| 44 | 061 §3 recorded: the drop ran on production at 10:54 BST on the 19th WITH its guard in the file (merged after 056 §1, before the app applied it); the guard did not raise. The 16 September premise in 061 §3 was wrong on the date; the exposure argument stands as "right, and the guard is what makes the next one not need luck" | builder | the ledger correction entry says so (#273) | **done pending merge** — 062 §1 repeats "both on 16 September"; the artefacts say the 19th (10:00:52 / 10:54:30 BST), on merge, guard in the file; 062's header on filing records the corrected state |
| 45 | **P0 (062 §2): build 18 is broken against production.** Verified from the IPA (EAS build `8f9293e5`, sha256 `c5c817c9…`, `CFBundleVersion` 18): the Hermes bundle carries `home_currency` and the exact hydration select `id, owner_id, name, destination, start_date, end_date, home_currency`. Paths at `f094c156`: the trips READ (`tripsHydration.ts:50`) — 42703, caught, the device's trips are kept and the server is never read again; the trip CREATE (`create-trip.tsx:255/265`) — 42703, "Couldn't create your trip just now"; cancel/delete name no column; add-expense/set-budget read the local row. Two paths, both known now; no other write path in build 18 names a renamed or dropped column | CTO (062 §2) | the ledger entry (#273) names the artefact and every path; **remedy is not production** — build 19 from current `main` is the CTO's decision (row 46) | **done pending merge** — the founder's own test of "create a trip" on build 18 is the second witness and is hers to report |
| 46 | Build 19 from current `main` — the honest remedy for row 45; **a decision, the CTO's, once §4 is answered** (062 §2). Precondition when it comes: the migrations reading in `docs/migrations-live-readings.md` (gate 0 of `submit-build.sh`), which is row 43's paste | CTO (062 §2) | the CTO says cut; then a build number in TestFlight | **in progress** — build 19's preconditions on main tonight (#274, #283, #284, #277 merged in the CTO's order); blocked on her paste (rows 43/48); the cut and the submission are the CTO's (070 §A/§C) |
| 47 | 062 §3 — **a rename is two deployments, never one**, with its trigger (a client you cannot update in lockstep reads the column), in `ENGINEERING.md` and on the red-lane checklist; the CTO's own error beside the reset in the ledger; the gate's founding incident in `docs/migrations-live-readings.md` | CTO (062 §3–§4) | the three files on `main` (#273 `cae376e`, `b58ca43`) | **done pending merge** |
| 48 | 064 item 2 — the migrations reading (the 44-row list) pasted into `docs/migrations-live-readings.md`, dated and signed; the CTO-sighted facts (both new versions present) recorded as a sighting, not a reading, until her paste lands | CTO (064 §B 2) | the gate reads green on `main` for the first time | **held by founder** — asked once 2026-09-21 |
| 49 | 064 item 3 / 065 §3 — the client–schema correspondence for build 19 by hand: every column the build names (reads and writes) against the live column reading AND the persisted store's field names; a table, reported before anything is cut | CTO | the table in the reply and in `docs/release-notes/build-19.md` | **done** — #274 merged 3e9118b |
| 50 | 064 item 4 — the gate behind item 3: red when the build names a column the live reading does not carry, or the reading is missing/stale; in the composite and first in `submit-build.sh` beside the migrations gate; red demonstrated from a fabricated reading | CTO | the gate on `main` with its fixture | **done** — #274 merged; the gate is in the composite (21 gates) |
| 51 | 064 item 5 / 065 §3 — the fallback: network-unavailable (quiet, expected) separated from server-rejected (an incident, reportable); the hydration fallback stops being sticky | CTO | the two messages in the tree with tests; the retry path | **done** — #284 (supersedes #275) merged 83ac8c5 |
| 52 | 064 item 7 — the mechanism audit: for every path that changes what runs (app build, functions, storage policies, website, mail templates, branch protection, …) established-with-artefact or assumed | CTO | the table in `docs/mechanism-audit.md` and in the report | **done** — #278 merged 085e403; second pass in #289 2094414 |
| 53 | 065 §2 — the third client: build 19's read-time migration of the on-device store from `home_currency` to `currency`, tested against a store snapshot written by build 18's code at `f094c156` (not a fixture written today); the rule names the on-device store as the third reader | CTO (065 §2) | the test and the snapshot on `main`; the clause in `ENGINEERING.md` | **done** — #283 (supersedes #276) merged 4ecb696; the union check run retroactively: 33 of 33, 36 of 36 |
| 54 | 064 item 8 — Supabase Preview as a required check: before-reading taken 2026-09-21 (four contexts); **the check on a PR head is `skipped`, 0 s — nothing ran; the apply is the `success` on the `main` squash, after the merge.** A skipped required check cannot gate what it never ran on | CTO (063 §2, 064 item 8) | the CTO's answer to the parked question | **closed** — 066 §2: not a control but a label; the gap already closed by the RLS live job; what Preview would add needs branching (a plan change); the residual risk and its two guards recorded in ENGINEERING (#289) |
| 55 | 064 item 9 / 063 §1 — the red-lane checklist gains deployment conditions (client compatibility, the build in hands, the rollback), the CTO's to answer per migration in writing; a merge-time check that a migration PR's body carries them | CTO | the checklist in `ENGINEERING.md`; the check with its fixture | **done** — #279 merged 895a484; extended by #288 (the clearance) |
| 56 | 064 item 10 — activity keying, implementation; PR left open for the CTO's read, not merged | CTO | the PR open, CI green, the seven `it.failing` tests flipped with both runs cited | **done** — #280 → #293 merged cec3310 (re-carried after #286; union check 25 of 25), cleared 071 §2 on two conditions: the delete path tested through deleteTrip; the persisted shape — not persisted, pinned |
| 57 | 064 item 11 / row 35 — the founder's surname as an Apple sign-in fixture in `social.test.ts` → a name with no connection to anyone real | CTO | the fixture renamed on `main` | **done** — #281 merged d7720d8 |
| 58 | 065 §1 — remove the CLI link (`supabase/.temp`) on this Mac; record the removal | CTO (065 §1) | the directory gone; the ledger line; inventory row | **done** — deleted 2026-09-21 07:51:05 BST; inventory row 34 and the ledger in #282 |
| 59 | 063 §1 — the ledger records the CTO's structural error (red-lane migrations cleared without establishing what merging does) and that the migrations-live gate was built on an invented premise and was valuable by luck, not prescience; 063 §3's clause (the guard was in place, its proof was not) | CTO (063) | the entries on `main` | **done** — #282 merged b9b983a |
| 60 | `scripts/merge-gate.sh` calls itself "the only sanctioned way to merge" and uses `--merge`; the rulebook's written procedure and the fortnight's merges squash, via the builder's scratchpad `merge-one.sh`. Two routes, one stale header — reconcile or retire one | builder (found 2026-09-21) | one route, named in `ENGINEERING.md` | **closed (087 §2)** — merge-one.sh is the route: squash, all required checks, CLEAN only, the clearance gate, the ruling in the body; merge-gate.sh goes on the Phase 1 retirement list |
| 61 | **FINDING (064 item 7):** `main` requires no pull-request review — `required_pull_request_reviews: null` (API, 2026-09-21). The CTO's clearance is a convention held by the builder's discipline, not by construction. Closing it is a branch-protection change and needs a reviewer account in the loop | builder (found 2026-09-21) | the CTO's decision; if closed, a before/after API reading in `docs/branch-protection-readings.md` | **closed by 066 §3** — not a required review (deadlock) but a check: '## CTO clearance' naming the ruling, read fresh at merge — recollection, not authority (#288) |
| 62 | 064 item 5's "it has to reach us": the rejected banner copies a report; a `mailto:` prefilled to `brand.privacyContactEmail` would make it one tap and send — a second use of the address ruling 17 admitted for one control | builder | the CTO's yes or no | **closed — no** (066 §4): a channel with a legal deadline does not share an inbox with one that has none; the clause in ENGINEERING (#289) |
| 63 | The day's merges: #274, #275, #276, #277 are build 19's preconditions and are not migrations; ENGINEERING's DoD requires an explicit clearance per merge, 064 §C reserves only migration merges. Parked rather than guessed. Note: #274, #275 and #276 each register files in `src/__tests__/trip-currency-name.test.ts` in the same region — the second and third to merge will need the superseding-PR procedure | builder | the CTO says which merge, in what order | **done** — merged in the CTO's order: #274, #283, #284, #277, #278, #279, #281, #282; #280 open for the read 
| 64 | The keep-alive: workflow and both secrets deleted; the list read back empty (`total_count = 0`, 2026-09-21 19:51 UTC); audit row 19 rewritten (never succeeded, 18 of 18); inventory row 36 states the pause risk | CTO (067 §1, 069 §1, 070 §B.2) | the secrets list empty; the workflow gone from `main` | **done** — #289 |
| 65 | **FINDING — unattended failure reached nobody (069 §2, audit row 24):** scheduled runs attributed to her account; zero watchers, owner included; the email setting unreadable. Remedy: `unattended-failure-alarm.yml` (#285) — an issue assigned to the owner on any failed unattended run, read back by the step. **Demonstrated through the real path: issue #290, 2026-09-21 20:37 UTC, assigned, closed with the demonstration named** | CTO (067 §3, 070 §B.4) | the issue; **and her word that the email reached her inbox — the one unconfirmed link** | **done — established end to end 2026-09-21 (072)** — her inbox received the email 21:37 BST; limitation recorded: her client flagged the sender; the safe list is the mitigation, a per-user setting no token can read; the last link cannot be monitored from here (audit row 24) |
| 66 | 070 §B.6 — conversion v1 built: a rate she types, per trip, on the device, display-only, its rate and date beside every figure; on the Budget page she reaches (the hub at `app/budget` is an orphan — the route gate) | CTO | #286 read and cleared; the `## CTO clearance` section added to its body (the gate reads it as red lane: money) | **done** — #286 merged ee0e79b through the clearance gate (red lane: money; ruling 071 in the body; the merge tool re-read it) — the gate's first real use (076 §2, in the ledger). Condition met: independent rounding pinned as deliberate; the display-only registry accepted (073 §2) |
| 67 | 070 §B.7 — rows 8 and 10 (the FK map from the catalog; the erasure residuals) are erasure work: §C's "the erasure in any form" — not tonight | builder | the CTO says which, in daylight | **held by CTO** |
| 68 | A commit-body line beginning `#286` was silently stripped by git (lines starting with `#` are comments under the default cleanup) on `f6c712f`; the sentence lives in #288's body. Rule: never begin a commit-body line with `#`. Candidate for the commit-message discipline in ENGINEERING | builder | the rule where commits are written | **CORRECTED — struck, not rewritten (073 §1):** ~~a commit-body line beginning #286 was silently stripped by git~~ — **it was not.** The line sits in f6c712f at body line 15. What omitted it was commitlint's echo of the message (its parser's comment character), read as the commit. The --cleanup ruling is withdrawn; nothing changed in the tooling. The true instance is in ENGINEERING's interpretation table with the right medium, and the clause *a tool's rendering of an artefact is not the artefact* sits beside the founding rule 
| 69 | **The orphaned hub (075 §1):** `app/budget/index.tsx` is unreachable — every reference is a back-fallback (route-reachability baseline), `app/budget/balances.tsx` beside it likewise; dead screens are eventually shipped by someone who assumes they work. **Recommendation:** retire both screens — `app/trip/budget.tsx` is the page she reaches and now carries edit/delete and conversion; retarget the ten back-fallbacks to `/trip/budget`; the baseline shrinks by two named rows (the honest direction); the duplicated `confirmDeleteExpense` closure the baseline records goes with the copy that would have gone stale. One PR, held for the read | builder (found 2026-09-21) | the CTO's yes; then the PR | **held by CTO** |
| 70 | **075 §2 — `expenses.converted_amount` and `fx_rate`.** Code side established and pinned: nothing assigns them; two RPCs would accept them and have no caller. **The reading is hers:** `select (select count(*) from public.expenses) as expenses, (select count(*) from public.expenses where converted_amount is not null) as converted_amount_set, (select count(*) from public.expenses where fx_rate is not null) as fx_rate_set;` — counts only. Zero → the migration (guard raising on any non-null row; both RPCs re-created without them; the drop) is written and opened with its deployment conditions; not zero → the design says why v1 does not use them | CTO (075 §2) | her three counts; then the PR, or the paragraph | **held by founder** — the reading |
| 71 | 075 §3 — the trip-delete sheet says what the only irreversible action is, what is inside (counted), that it goes for everyone, and how to keep a copy — #294 | CTO | the CTO's read | **held by CTO** — open for the read |
| 72 | **Filing the reading (075 §1).** Complete and self-verified on her side (227 columns; 44 migrations); the raw rows have not reached this repository — the CTO relayed the summary. Both docs now prescribe the filing shape (the query in a fence, the rows, the count row) and the gates assert count, floor and query text. **What closes it: the paste itself** — from her, or from the CTO forwarding it verbatim — filed under her name and 2026-09-22 | builder | both `## Reading` sections present; both gates green; `main`'s composite green for the first time | **done** — the text supplied and attested (082 §0) and filed in both readings docs (#307 on main); both gates green on the real docs (client-schema 227/227 columns, migrations 44/44); fixtures 43/43 |
| 73 | 075 §2's siblings: `receipt_path`, `original_email_path`, `raw_source_ref` ×2, `trips.email_slug` — dormant by design; inventory row 37 says so (#297, on main at 5189e50). Not urgent; no action until a feature reaches them | builder | — | **done** — the row ||
| 74 | **Day brief §1 (2026-09-22) — build 19 stopped at the composite:** `migrations-live` and `client-schema` both MISSING because the reading's raw rows never reached the repository (row 72). The control is taken: build 18's bundle has `home_currency` ×1 (UTF-8), ×0 (UTF-16LE), the old select ×1, the new select ×0; IPA sha256 `c5c817c9…3ca685`, CFBundleVersion 18, `EXUpdatesEnabled false`. Build 19's proof command is the same four greps | CTO (day brief §1) | row 72 filed → both gates green → the cut, with the four counts 0·0·0·1, the sha256 and CFBundleVersion in the report | **blocked on the live RLS reading for build 19 (row 87)** — composite alone on clean main `8d39087`, 2026-09-23 07:45 BST: 21 gates, 0 red, 89 s (99 s with `npm ci`); notes gate 19 OK; `rls-live-reading-gate.js 19` FAIL "no live RLS reading for this build" — the one red left, and it is the founder's paste |
| 75 | **Day brief §2 — the row-count reading** (`docs/row-count-readings.md`): 27 rows in 074's self-verifying shape, incl. `auth.users_unconfirmed` and `storage.objects`; the builder has no production read path | builder → founder | the paste, filed under her name and date; a table with no writer in C8 and a non-zero count is a finding | **held by founder** — the paste |
| 76 | **Day brief §3 — #299, §6 block E** (`inbound_emails.sender` keyed on the address; the raw source and parsed wallet items with it; pgTAP twin, plan 12) | builder | the CTO's read; two decisions inside it: `message_id` (leave or `'erased:'||id`) and whether E becomes a migration into `private.erase_user` | **merged 2026-09-23** (082 §4 — no migrations file; three files) |
| 77 | **Day brief §4 — the OTP probe's cleanup** (`docs/probes.md` P1): step 1 is six counts she reads; step 2, if the row exists, is `erase_user_admin(<uuid>)` by her as owner, after the CTO clears it. Resend's suppression entry stays | CTO → founder | the counts filed; the erasure recorded in the ledger; P1's row closed with observed beside predicted | **held** — the read, then the clearance |
| 78 | **Resend → Webhooks** — the one tracking sub-item not yet read (TRA §7 review row) | founder | a dated row in the TRA saying what the page shows | **held by founder** — she has read three Resend artefacts today; the fourth (webhooks) came back EMPTY from the CTO on the night of the 22nd (081 §0) — **CLOSED** |
| 79 | **#300 — itinerary keying** (§6.3 of the keying order; version 1→2 with the build-18 snapshot; 7 defining tests, 4 reds; suite 2,999 green) | builder | the CTO's read, as #280 had | **CLEARED (082 §2)** — merges after the cut, per the 082 §7 order, not before |
| 80 | **Row 8 (029 §3) is satisfied by an artefact:** `supabase/tests/erasure_fk_interlock.test.sql` asserts every FK's delete action from `pg_constraint` on every CI run; its header records its first red (the eight GoTrue tables, 2026-09-16) | builder | the CTO closes row 8 on it, or says what is missing | **for the CTO** |
| 81 | **#302 — wallet keying** (§6.4; version 1→2 with the build-18 snapshot; 6 defining tests, 4 reds; suite 2,999 green; independent of #300, re-carry on whichever merges second) | builder | the CTO's read, as #280 had | **CLEARED (082 §2)** — merges after the cut, after #300, re-carried with the union check |
| 82 | **#303 — the budget and brief keying designs** (§6.5 B over A, projections deleted, money arithmetic untouched, a money control among the reds; §6.6 the brief's duplicated six deleted, archive keyed). Three decisions in §6.5, one in §6.6 | builder | the CTO's read and the four decisions; then the budget keying is written and opened HELD (money), the brief's opened for read; the lock comes off with the last | **ruled in one pass (082 §3, four decisions); BUILT as #309 (row 88) — HELD, money.** #303's only file is carried by #309 (§6.5 built, §6.6 corrected), so #303 is superseded — noted on the PR; it closes on the CTO's word |
| 83 | **The query text as run (row 72's last condition).** The reading's rows are filed and verified (227 split = 227 stated; 44 = 44; both set-equal to the tree); both gates are red on exactly one condition — QUERY, the reading records no sql fence — because an editor export does not carry the query text and ruling 077 §1 pins it. **One paste unblocks build 19: the query text as run on the 22nd, from the SQL editor's history** | CTO | the fence filed beside the rows → both gates green → the cut | **done** — supplied and attested (082 §0); filed (#307); both gates green |
| 84 | **#306 — the guarded drop of `expenses.converted_amount` / `fx_rate`** (081 §5): guard + both RPCs re-created without the columns + drop; pgTAP proof red/green; lockstep. Clearance gate green on its shape; **does not merge until build 19 is in her hands and confirmed working** | builder | the CTO's word after that confirmation | **RED LANE — HELD** until build 19 is confirmed from her phone (082 §5) |
| 85 | **The cleanup of the five test accounts** (`docs/cleanup-plan-test-accounts.md`, 081 §8): proposed only; the sentinel is DO NOT DELETE; runs after build 19 and after the erasure file | CTO → founder | clearance, then her hands, one account at a time, counts before and after | **RED LANE — proposal filed** |
| 86 | **`auth.audit_log_entries` 0 — count, not reading** (081 §4): the two-boolean catalog query is in `docs/row-count-readings.md` for the next reading (names and integers only) | founder (next reading) | the three integers filed; P1's audit row closed either way | **held — the next reading** |
| 87 | **The live RLS reading for build 19** — `docs/rls-live-readings.md` needs `## Reading for build 19 — YYYY-MM-DD — read by <name>`: the doc's three catalog queries, dated after the newest applied migration, the table rows and the Plan line; `scripts/rls-live-reading-gate.js 19` refuses the cut without it (only build 18's reading exists, 2026-09-14) | builder → founder | the paste filed → the gate green → `eas build` → the IPA read (0·0·0·1) → `submit-build.sh 19` | **route ruled (086 §2)** — the grouped query, after #312 merges; Francesca runs it, exports CSV, pastes it into the builder's chat; filed verbatim; the gate; then the cut (row 98) |
| 88 | **#309 — the budget keying, built to the four rulings** (082 §3: B; the three scalars deleted in the migration and derived at read, both screens tested with a red; alerts as a keyed row, the `thresholds_fired` divergence written as deliberate with its reconciling sentence; the money control among seven reds; third client 1,054 bytes through the real boot, 14/14 with the totality set; suite 206/206, 3,013) | builder | the CTO's read; merges after #300 and #302, re-carried with the union check | **done** — cleared at 8ea4438 (098): (a)–(f) met; merged via merge-one.sh |
| 89 | **The store read-guard follow-up** (082 §2): a defensive read in each keyed store that treats a non-array collection as empty and records it, with a test and a red for each store; the rule filed in ENGINEERING | CTO → builder | the PR, and the CTO's read | **#310 open — for the CTO's read** (cityLegs + the mechanism + the rule in ENGINEERING; 9 + 2 tests, reds 8/34 and 1/34, suite 3,011). The second half — itinerary, wallet, budget — on the day #300/#302/#309 land, a second PR with the same boot test each |
| 90 | **The brief keying** (§6.6, corrected: `briefStore` is not persisted — no migration, no third client, the absence pinned; the six duplicated fields deleted from the type, the archive keyed, `lastShownISO` per trip) | builder | the PR — and the one-trip lock comes off in the same PR as the last keying, never before | **#311 open, DRAFT since 24 Sept — must not merge before #300 and #302.** Correction at the top of its body: the branch lacks the wallet and itinerary keying (tripId ×0 and ×0 on it), so the lock removal alone would let a second trip show the first trip's bookings. Order: cut → #300 → #302 → #309 → #311 re-carried, plus a guard added at the re-carry (no trip-scoped resource except currentTrip may be purged-when-current), shown red then green. Suite on the branch 3,025 (the commit body's 3,037 is wrong) |
| 91 | **Saved places outlive a deleted trip.** `savedPlacesStore` rows carry `tripId` (`placesForTrip`), but `deleteTrip` (`src/features/trips/tripLifecycle.ts`) never purges them, and `SAVED_PLACES_STORE_KEY` is in neither `PERSISTED_STORE_KEYS` nor `TRIP_SCOPED_RESOURCES`. That is the vacuous-lockstep instance ENGINEERING already names, still open. Found 24 Sept by a read-only mapping and confirmed by grep (0 references in `tripLifecycle.ts` on `main`) | builder | a `purgeTripSavedPlaces` in `deleteTrip`, the key in both registries, a test with a red | **superseded by row 103** (086 §6 B3: saved places in the sign-out reset, delete-trip and the export) |
| 92 | **The RLS live-reading query, GROUPED (083 §1)**: one row per table, `relrowsecurity` read per table from `pg_class`, the policies (name, cmd, roles, USING, WITH CHECK) aggregated into the detail column, **the result's own row count as the first row** (~24 rows). Catalog names and expressions only, so no person's identifier by construction. Prescribed in `docs/rls-live-readings.md` first; the doc's fence and the text sent are one string (pinned like 077). `scripts/rls-live-reading-gate.js` must parse the grouped form, with a demonstrated red. Send her one line on why: a policy expression can name the renamed column | CTO | the doc carries the fence; the gate parses it (red shown); the exact text goes to the CTO, who relays it | **done** — #312; cleared to merge as it is (086 §2); proven on real Postgres (CI run 36040535333: 33 tables, 72 policies, gate OK) |
| 93 | **The Morning Brief's archive does not survive the app closing (083 §2).** `briefStore` is memory-only (`pinned-absences.md` row 23, on #311). **Intended or a defect? Nobody has said which.** Its own issue; not resolved inside the keying work | CTO | the CTO (or Francesca through the CTO) rules intended or defect; if a defect, a PR with persistence, migration, read guard and snapshot | **ruled (086 §4)** — a signpost to remove now (row 104), a feature later; issue #313 stays open as a post-launch feature |
| 94 | **#309's paste for clearance (083 §3)**: envelope shapes before and after plus the migration's full text; the four arrays and the three dropped scalars with each deriving line; the diff's file list plus the statement that `money.test.ts` and `state.test.ts` ran UNMODIFIED (checked 24 Sept: neither is in #309's 44-file diff); the money control red; the alerts red; the totality set, each with its red; **and the check that caught the mechanical-fixer class, with what was found, from the artefacts, not "it worked"** | CTO | the paste sent; the CTO clears or refuses | **done** — the paste sent 2026-09-24 (per-property reds re-run on f9bcb6f: 14 of 15 red, (1b) unpinned); cleared on five conditions (086 §3), row 99 |
| 95 | **084 §0 — the direct channel.** The CTO session ("Amiico CTO review") reports and rules directly to the builder; each message filed verbatim as the next ruling; manifests on every reply | CTO (084) | Francesca confirms the channel in the builder's chat | **done** — confirmed 2026-09-24; 084–086 filed |
| 96 | **084 §3 — (a)** would a reading from the doc's existing queries pass the gate on main; **(b)** is there a cheap seam for the rename-carries-through claim | CTO (084) | answered from artefacts | **done** — (a) yes, conditionally (simulated against main's gate: OK, exit 0); (b) the seam exists and is not built (086 §5) |
| 97 | **085 §2 — process freeze.** No new gates, registers, readings files, lint scripts, rulings-infrastructure or ENGINEERING.md sections; existing CI unchanged until the Phase 1 retirement list | CTO (085) | the Phase 1 retirement list | **standing** |
| 98 | **086 §2 — build 19's route.** Merge #312; the grouped query posted to Francesca from the merged doc's fence with its sha256, and the Plan line; her CSV export filed verbatim; the gate; a quoting red is reported with its first failing row, the input never adjusted; then the cut procedure unchanged | CTO (086) | build 19 submitted once, IPA read 0·0·0·1, notes pasted and recorded | **done 2026-09-25** — build 19 (71339ca, EAS 46b15327, 0.1.0 (19)) submitted once (4873b59a); notes pasted by Francesca 2026-09-25 and recorded verbatim; ledger entry (#317, main 4b3e9f3) |
| 99 | **086 §3 — #309's five conditions:** (a) property (1b) pinned, red then green; (b) the body corrected; (c) the two coercions by design accepted and stated with 086; (d) the F1 guard merged first; (e) the order #310 → F1 guard → #300 → #302 → #309 → #311 plus its guard, union check on every re-carry. Withdrawn on any edit to money.test.ts or state.test.ts, any change to the nine money functions, or the money control no longer reddening | CTO (086) | #309 merged with all five met | **after the cut — builder** |
| 100 | **086 §3 (d) — F1:** the boot purge and every boot-time reader of the budget envelope treat a missing or non-array collection as empty, retained and recorded (the #310 pattern); the TypeError shown red on the old code in a scratch tree, then green | CTO (086) | the PR merged before #309 | **after the cut — builder** |
| 101 | **086 §6 B1 — sign-out that cannot destroy by accident** (supersedes #178): a confirmation that says what lives only on this phone; ONE registry-driven reset of every persisted store and its listeners; proof: cancel keeps, confirm empties, a second account sees nothing | CTO (086) | the PR, with the simulator action and what was seen | **Phase 0, after the keying chain (087 §1)** — scope confirmed (087 §4): the in-memory reset of you, budget, wallet, itinerary, brief, activity, tripConversion, plus the confirmation; proof: a second account signed in WITHOUT restarting the app sees nothing of the first |
| 102 | **086 §6 B2 — money IDs and money input.** RED LANE: randomUUID ids; a read-time repair for duplicate ids, retained and recorded; ONE string decimal parser in add-expense, settle and set-budget, each listed input with its chosen outcome and why; test names to the CTO before merge | CTO (086) | the PR, cleared | **Phase 0** |
| 103 | **086 §6 B3 — saved places** in the sign-out reset, delete-trip and the export; the persisted-key list derived from the registry | CTO (086) | the PR | **Phase 0, after the keying chain; revised (087 §3)** — sign-out already clears saved places (session.ts:189, clearAllPersistedStores): that part becomes a test that reds if clearSavedPlaces() is removed; the work is delete-trip, the export, and PERSISTED_STORE_KEYS derived from the registry |
| 104 | **086 §6 B4 — the honesty sweep:** five named promises removed (incl. the Past briefs entry, 086 §4); every changed string listed across the six locales; the email's code line left | CTO (086) | the PR | **Phase 0** |
| 105 | **086 §6 B5 — PR triage:** eleven named PRs closed with a one-line reason each; anything worth keeping becomes an issue; #140 checked against #181; the live branches without a PR listed to the CTO before any deletion | CTO (086) | the closures, and the list sent | **Phase 0** |
| 106 | **086 §7 — the plan of record read against the tree:** every disagreement, one line each: what it says, what is true, the evidence | CTO (086) | the list sent | **open — builder** |
| 107 | **087 §1 — the order after build 19:** the keying chain first (#310 → F1 guard → #300 → #302 → #309 with 086 §3's conditions → #311 plus its guard), **build 20 cut as soon as the chain is in** (multi-trip, her top request); then B1–B5 on the keyed tree, **build 21** when they are in; one submission each, the full procedure each time | CTO (087) | build 20 and build 21 submitted | **after build 19 — builder** |
| 108 | **087 §5 — the crypto line clarified:** the Phase 1 removal item is SQLCipher / op-sqlite (op-sqlite `"sqlcipher": true`, @op-engineering/op-sqlite, @powersync/op-sqlite — required only by unreachable code), not expo-crypto, which B2 uses; whether randomUUID changes the export-compliance answer is not established and is settled by the Phase 4 IPA read | CTO (087) | the Phase 1 removal; the Phase 4 read | **recorded** |
| 109 | **088 §3 — driving her logged-in consoles:** the account of every page opened, tool used and anything rendered, from the artefacts; the standing rule until the catalogue-only role exists (her explicit ask per task; the SQL editor with a sha-checked catalogue-only query and the Backups page; App Store Connect's What to Test only; never table editor, Auth users, Logs, API keys, Storage, secrets, testers, billing, DNS; page text over screenshots; no setting changed) | CTO (088) | the account sent; the rule observed | **done** — the account sent from the transcript (41 browser actions, each result pattern-scanned); accepted as clean (089 §1), no breach-log entry; the standing rule in force |
| 110 | **088 §4 — a catalogue-only database role for CI** to take the RLS reading itself: reads pg_catalog and pg_policies, no select on any public, auth or storage table, a demonstrated refusal on a real table, its password created and put into a GitHub Actions secret by her hands only. RED LANE, design only | CTO (088) | the design sent with Phase 1 | **Phase 1 — design** |
| 111 | **091 §2 — from build 20, the pasted span of the notes is plain text:** no # or **; section titles and "1." checks as their own lines; "Right:" / "Wrong:" at a line start; one paragraph per line, no hard wraps inside a sentence; markdown the notes gate needs stays outside the span; a gate change is told to the CTO first, no renderer; check 3's apostrophe corrected ("You’re") | CTO (091) | build 20's notes written that way | **build 20 — builder** (the build-19 notes shipped as merged, 091 §1) |
| 112 | **092 §2 — how Francesca is reached:** every request for her goes to the CTO in the PARKED line of the builder's report (not to her directly, unless she is already in the builder's chat); the CTO batches them into one phone notification; the builder reports to the CTO after every milestone (each chain merge, build 20, each of B1–B5) and waits only on red-lane clearances | CTO (092) | — | **standing** |
| 113 | **092 §3 — sessions end:** on a restart, read 084–092 and the plan page, re-verify the state by command, and report "resumed at <state>" to the CTO first | CTO (092) | — | **standing** |
| 114 | **093 — #318 cleared on three conditions:** (a) RLS live and Quality green on its head, run ids in the body; (b) "## CTO clearance / ruling: 093" in the body; (c) the absence the 'n' class rests on (no auth.scim_users row: SCIM/SSO provisioning not configured) pinned as a row in docs/pinned-absences.md — how established, what ends it, what that obliges (a runbook step deleting the row; the class re-ruled). The CI CLI stays on `latest` deliberately (093 §1) | CTO (093) | #318 merged with all three met | **done** — #318 merged 497ce7c: run 36150622949 all four green (interlock ok on gotrue v2.197.0); clearance in the body; pinned-absences row 23 |
| 115 | **094 §3 — a merge order is not a review:** a PR the CTO has not named as read and cleared waits for the read, even inside a merge order the CTO wrote and even when green by path. The F1 guard PR, #300 and #302 (re-carried heads, union check) and #311 with its guard each get a named read; #309 is cleared by 086 | CTO (094) | — | **standing** |
| 116 | **095 §3 — #309 condition (f):** the re-carry REPLACES readBudgetState with a v2 read — guarded lists for budgets, expenses, settlements, alerts, each alert's thresholdsFired a guarded number list; M-style reds incl. "good v2 data records no fault"; test names and red matrix pasted with #309's paste | CTO (095) | #309 merged with (a)–(f) met | **with #309 — builder** |
| 117 | **095 §4 — the remaining reads:** one PR after #300 and #302 guards the boot reads of wallet, itinerary AND `you` (row 89's second half, `you` added), the same pattern, shared-options wiring and reds | CTO (095) | the PR, read and merged | **after #302 — builder** |
| 118 | **095 §5 — retain failure (094 §2b):** if `.retained.*` cannot be written, apply must NOT proceed — the store stays unhydrated in memory, the key untouched, the failure recorded and surfaced | CTO (095) | a Phase 1 early PR | **Phase 1** |
| 119 | **098 §4 — build 20:** its expect file derived from the tree, not memory; its notes the first in plain text (091 §2); the five checks include creating a second trip AND adding an expense to trip B while trip A's total stays the same (the money control, by her hand) | CTO (098) | build 20 submitted | **after #311 — builder** |
| 120 | **099 — the read-guard PR (095 §4) with `you`'s nested records:** wallet items; itinerary days/items/suggestions; you emergencyContacts, profile, and privacy/consent/notifications, each non-object record read as the private default, recorded and retained; a test that each fallback is the private choice; M-private red. **Its premise differs from the tree:** EMPTY_YOU_STATE does not exist, and initialYouState's notifications default ON (morningBrief, urgentChanges) — reported, the nested half held | CTO (099) | the PR, read and merged | **builder — building** (100: YOU_READ_FALLBACKS per field — profile REAL_YOU_PROFILE, contacts [], privacy off, consent not granted, notifications all false; first-run defaults unchanged, notifications ON is a plan-page question) |
